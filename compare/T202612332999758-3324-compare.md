# T202612332999758-3324 与人工指定历史候选 T202510213995926-2475 对比分析报告

- A（目标作品）：`T202612332999758-3324`
- B（人工指定历史候选）：`T202510213995926-2475`
- 候选身份：年份=unknown、学校=unknown、队伍=unknown
- 候选身份来源：`metadata_unavailable`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **48%**，对应“中度相似”。

模块级对照覆盖 **10** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | 进程间通信 | 78% | 做法接近 |
| 2 | 信号机制 | 75% | 做法接近 |
| 3 | 文件系统 | 65% | 目标相同、实现路径不同 |
| 4 | 驱动框架 | 60% | 目标相同、实现路径不同 |
| 5 | 内存管理 | 55% | 目标相同、实现路径不同 |
| 6 | 进程与任务调度 | 55% | 目标相同、实现路径不同 |
| 7 | 启动流程 | 50% | 目标相同、实现路径不同 |
| 8 | 系统调用层 | 45% | 目标相同、实现路径不同 |
| 9 | 网络 | 10% | 独立设计 |
| 10 | hal | 0% | 仅目标作品实现 |

## 三、逐模块源码证据

### 3.1 进程间通信：78%

**对照判断：**做法接近。 共享内存/futex（快速用户态互斥量，即用户态原子操作配合内核等待唤醒的同步机制）/管道核心机制一致，但 futex 键和共享内存映射路径有差异。

#### 相似点

- **共享内存段表管理**
  - A: `os/src/ipc/sysv/shm.rs:72-76` — ShmObj 含元数据与 PageCache，Arc 共享
  - B: `os/src/mm/shm.rs:22-25` — ShmManager 用 RwLock<BTreeMap> 保护段表
- **futex 地址键等待队列**
  - A: `os/src/syscall/futex.rs:389-392` — FutexHashKey 区分私有/共享
  - B: `os/src/futex/futex.rs:80-85` — FutexKey 用 ptr/aligned/offset 三元组
- **管道环形缓冲区**
  - A: `os/src/fs/pipefs.rs:24` — PipeMeta 含环形缓冲与等待队列
  - B: `os/src/fs/pipe.rs:35-39` — PipeInode 经 Arc<Mutex<PipeRingBuffer>>共享

#### 主要差异

- **futex 键构造策略**
  - A: `os/src/syscall/futex.rs:389-392` — 私有用 mm+虚拟地址，共享用物理地址
  - B: `os/src/futex/futex.rs:80-85` — 统一 ptr/aligned/offset，ptr 可为 mm 或 inode
- **共享内存映射路径**
  - A: `os/src/syscall/ipc/sysv.rs:83-87` — shmat 经 alloc_anon_area 映射 UserVmFile::Shm
  - B: `os/src/mm/shm.rs:400-414` — 调用 MemorySet::attach_shm_segment 直接建页表映射

### 3.2 信号机制：75%

**对照判断：**做法接近。 共享 POSIX（可移植操作系统接口，即 Unix 类系统通用接口规范） 信号架构与 trampoline 投递方式，差异在实时队列与多架构支持。

#### 相似点

- **信号动作表管理**
  - A: `os/src/signal/action.rs:56-63` — KSigAction 包装用户动作并区分默认/自定义
  - B: `os/src/signal/sig_handler.rs:8-17` — SigHandler 固定长度数组存储每个信号动作
- **用户态 trampoline 投递**
  - A: `os/src/task/signal.rs:122-170` — 修改 trap（陷入，即异常、中断或系统调用进入内核的统一路径） context 使 sepc 指向 handler，ra 指向 sigreturn
  - B: `os/src/signal/mod.rs:246-254` — 修改 ra/sp/sepc/a0 跳转到 handler 和 sigreturn_trampoline
- **进程级信号分发**
  - A: `os/src/task/signal.rs:61-77` — 遍历线程组找第一个未阻塞线程投递
  - B: `os/src/task/signal.rs:12-32` — 区分线程级和进程级，进程级广播到线程组并唤醒

#### 主要差异

- **实时信号队列支持**
  - A: `os/src/signal/manager.rs:15-22` — BTreeMap 按信号号分队列，支持多实例排队
  - B: `os/src/signal/sig_struct.rs:51-68` — 使用 trailing_zeros 选最小未屏蔽信号，未实现实时优先级
- **多架构支持**
  - A: `hal/src/component/signal/riscv64/mod.rs:65-83` — UContext 仅提供 riscv64 寄存器保存恢复
  - B: `os/src/signal/sig_frame.rs:305-312` — SigContext 按架构条件编译，支持 riscv64 和 loongarch64

### 3.3 文件系统：65%

**对照判断：**目标相同、实现路径不同。 共同具备VFS（虚拟文件系统，即统一不同文件系统的接口层）抽象层与ext4根挂载及伪文件系统，但核心trait结构、缓存机制与挂载管理实现明显不同。

#### 相似点

- **VFS抽象层**
  - A: `os/src/fs/vfs/inode.rs:109-114` — Inode trait定义读写等核心文件操作，作为VFS抽象核心
  - B: `os/src/fs/mod.rs:11-27` — 通过子模块组织dentry/inode/file等抽象层
- **挂载ext4根文件系统**
  - A: `os/src/fs/mod.rs:1-120` — FS_MANAGER注册并挂载ext4_native为根文件系统
  - B: `os/src/fs/mount.rs:181-216` — do_ext4_mount创建根dentry/inode并挂载ext4
- **伪文件系统集成**
  - A: `os/src/fs/mod.rs:1-120` — 在根挂载后依次注册devfs/procfs/tmpfs
  - B: `os/src/fs/mount.rs:181-216` — 初始化devfs/procfs/tmpfs子文件系统

#### 主要差异

- **VFS核心抽象不同**
  - A: `os/src/fs/vfs/inode.rs:109-114` — 以单一Inode trait为所有文件系统统一接口
  - B: `os/src/fs/mod.rs:11-27` — 采用Dentry/InodeOp/FileOp三组trait分层抽象
- **缓存策略不同**
  - A: `os/src/fs/ext4_native/native/inode.rs:129-140` — 基于PageCache的脏页刷新机制
  - B: `os/src/fs/dentry.rs:539-563` — 基于Arc引用计数的dentry缓存清理
- **挂载管理结构不同**
  - A: `os/src/fs/mod.rs:1-120` — FS_MANAGER为文件系统类型注册表（BTreeMap）
  - B: `os/src/fs/mount.rs:36-62` — VfsMount与Mount树结构管理挂载点

### 3.4 驱动框架：60%

**对照判断：**目标相同、实现路径不同。 两仓均以块设备trait为核心并支持VirtIO（虚拟输入输出接口，即虚拟机中使用的标准设备协议），但设备选择机制、并发/缓存策略及初始化方式差异显著。

#### 相似点

- **块设备抽象接口**
  - A: `os/src/drivers/block/mod.rs:30-42` — BLOCK_DEVICE 为 Arc<dyn BlockDevice> 全局单例
  - B: `os/src/drivers/block/block_dev.rs:5-11` — BlockDevice trait 定义 read/write_blocks 接口
- **VirtIO 块设备支持**
  - A: `os/src/drivers/block/mmio_blk.rs:81-109` — VirtIOMMIOBlock 实现异步块读
  - B: `os/src/drivers/block/mod.rs:12-17` — virt 特性下使用 arch::VirtIOBlock
- **块设备自检函数**
  - A: `os/src/drivers/block/mod.rs:58` — 触发写后读自检，断言一致
  - B: `os/src/drivers/block/mod.rs:28` — 读写自检，会破坏文件镜像

#### 主要差异

- **设备选择机制**
  - A: `os/src/drivers/block/mod.rs:30-42` — 运行时按 DEVICE_MANAGER 按名查找并架构硬编码
  - B: `os/src/drivers/block/mod.rs:12-17` — 编译期 cfg 特性确定具体类型
- **并发与缓存优化路径**
  - A: `os/src/drivers/block/pci_blk.rs:33-38` — SMP 下用 SpinNoIrqLock 串行化 virtqueue
  - B: `os/src/drivers/block/block_cache.rs:127-134` — 缓存淘汰依赖 Arc 强引用计数，延迟写回
- **初始化方式**
  - A: `os/src/drivers/block/mod.rs:30-42` — 按架构硬编码 sda0/sda1 设备名，无设备树
  - B: `os/src/drivers/mod.rs:52-55` — FDT（扁平设备树，即启动时传递硬件信息的数据结构） 设备树解析 address_cells 等属性

### 3.5 内存管理：55%

**对照判断：**目标相同、实现路径不同。 职责同为内存管理，BTreeMap 管理/文件映射/缺页机制相似，但帧分配器、堆分配器与地址空间抽象差异显著。

#### 相似点

- **以 BTreeMap 按虚拟页号管理映射**
  - A: `os/src/mm/vm/mod.rs:153-162` — UserVmArea 用 BTreeMap 管理已映射帧
  - B: `os/src/mm/memory_set.rs:88-95` — MemorySet 以 BTreeMap<VirtPageNum, MapArea> 管理映射区域
- **支持文件映射**
  - A: `os/src/mm/vm/mod.rs:153-162` — UserVmArea 支持文件/共享内存/匿名三种映射来源
  - B: `os/src/mm/area.rs:178-184` — 文件映射通过 file.get_pages 批量获取页缓存建立映射
- **缺页处理机制**
  - A: `os/src/mm/vm/kvm/riscv64.rs:200-200` — 内核虚拟内存区域缺页时从文件按需映射
  - B: `os/src/mm/memory_set.rs:2068-2077` — 检查写时复制 (COW（写时复制，即共享内存页只在写入时复制）) 标志，按引用计数决定复制或清除 COW

#### 主要差异

- **帧分配器实现方式**
  - A: `os/src/mm/allocator/frame_allocator.rs:33-57` — 基于 bitmap_allocator 位图分配，带对齐处理
  - B: `os/src/mm/frame_allocator.rs:79-88` — 栈式分配，回收栈复用 O(1)
- **内核堆分配算法**
  - A: `os/src/mm/allocator/slab_allocator.rs:135-152` — 自研 slab 分配器，13 档缓存，大对象降级帧分配
  - B: `os/src/mm/heap_allocator.rs:7-19` — 使用 buddy_system_allocator 的 LockedHeap<32>
- **地址空间抽象结构**
  - A: `os/src/mm/vm/mod.rs:130-130` — UserVmArea 表示用户虚拟内存区域，另有 KernVmSpace
  - B: `os/src/mm/memory_set.rs:80-80` — MemorySet 统一管理整个进程地址空间

### 3.6 进程与任务调度：55%

**对照判断：**目标相同、实现路径不同。 两者均实现多核 per-core 调度、RT/普通双队列与 vruntime，但 A 为 async 协程模型，B 为传统 switch，且负载均衡机制差异显著。

#### 相似点

- **多核 per-core/per-hart 调度队列**
  - A: `os/src/task/schedule.rs:147-152` — 调度闭包决定任务入哪个核的哪个队列
  - B: `os/src/task/scheduler.rs:56-60` — 每个 hart 独立维护一个调度器实例
- **RT 优先级与普通任务分离**
  - A: `os/src/executor/mod.rs:95-116` — RT 任务入专用 rt 队列，按优先级分流
  - B: `os/src/task/scheduler.rs:87-100` — fetch_task 优先取实时任务再取 CFS 任务
- **vruntime 加权公平调度**
  - A: `os/src/task/task.rs:271-275` — vruntime 按 cgroup 权重加权累加
  - B: `os/src/task/manager.rs:289-299` — CFS 模式下更新 vruntime 执行起点

#### 主要差异

- **调度模型：协程 future vs 传统 switch**
  - A: `os/src/task/schedule.rs:102-122` — 用户任务为 future，poll 驱动 trap 循环
  - B: `os/src/task/scheduler.rs:183-187` — schedule() 阻塞式进行上下文切换
- **多核负载均衡机制**
  - A: `os/src/executor/mod.rs:95-116` — 负载溢出到 stealable 并唤醒空闲核偷取
  - B: `os/src/task/task.rs:492-493` — 子任务继承父 cpu_id，不参与迁移
- **内核上下文保存方式**
  - A: `os/src/task/schedule.rs:46-66` — UserTaskFuture::poll 切换 EnvContext
  - B: `os/src/task/context.rs:27-34` — TaskContext 保存 tp，__switch 汇编恢复

### 3.7 启动流程：50%

**对照判断：**目标相同、实现路径不同。 共同点多核分流与 loongarch64 DMW/TLB 处理，但入口抽象、唤醒机制与初始化位置差异明显，综合相似度约 50。

#### 相似点

- **多核启动主从核分流**
  - A: `os/src/main.rs:119-142` — main 区分 first 与后续 hart，分别执行完整/最小初始化。
  - B: `os/src/main.rs:109-118` — IS_BOOT 原子标志分流，主核完整初始化，从核最小初始化。
- **loongarch64 DMW 窗口配置**
  - A: `hal/src/component/entry/loongarch64.rs:9-21` — 入口汇编配置 UC/CA 窗口映射物理地址。
  - B: `os/src/arch/la64/entry.S:9-22` — 通过 DMW 窗口实现物理地址直接映射。
- **TLB 缺失处理路径**
  - A: `hal/src/component/entry/loongarch64.rs:119-130` — tlb_init 配置 Sv48 并安装 _tlb_fill 汇编处理。
  - B: `os/src/arch/la64/mod.rs:32-41` — bootstrap_init 配置 TLB 重填入口。

#### 主要差异

- **riscv64 入口地址处理**
  - A: `os/src/main.rs:217-230` — naked pre_main 手动保存寄存器并切换栈到内核栈底。
  - B: `os/src/main.rs:74-84` — fake_main 将 sp 加上 KERNEL_BASE 偏移后跳转。
- **多核唤醒机制**
  - A: `os/src/main.rs:105-112` — processor_start 遍历核并发送 IPI 唤醒其他 hart。
  - B: `os/src/main.rs:109-118` — 从核自检 IS_BOOT 原子标志决定是否主核。
- **loongarch64 初始化位置**
  - A: `hal/src/component/entry/loongarch64.rs:9-21` — 入口汇编完成 CSR、DMW、栈切换后跳 rust_main。
  - B: `os/src/arch/la64/mod.rs:32-41` — bootstrap_init 在 C 层配置浮点、时钟、分页与 TLB。

### 3.8 系统调用层：45%

**对照判断：**目标相同、实现路径不同。 职责同为 syscall 分发与 trap 集成，但 A 用枚举+异步+hal 抽象，B 用常量+同步+裸指针，机制差异显著。

#### 相似点

- **唯一 syscall 分发入口**
  - A: `os/src/syscall/mod.rs:376-389` — async syscall() 统一分发入口，未知号返回 ENOSYS
  - B: `os/src/syscall/mod.rs:1-5` — 模块文档声明 syscall() 为唯一入口
- **统一 errno 错误码返回**
  - A: `os/src/syscall/mod.rs:374` — SysResult=Result<isize, SysError>
  - B: `os/src/syscall/errno.rs:1` — SyscallRet=Result<usize, Errno>

#### 主要差异

- **syscall 号映射方式**
  - A: `os/src/syscall/mod.rs:14-18` — SyscallId 枚举+strum::FromRepr 映射
  - B: `os/src/syscall/mod.rs:237-270` — 硬编码 SYSCALL_ 常量
- **同步 vs 异步 syscall 执行**
  - A: `os/src/trap/mod.rs:64-81` — user_trap_handler await syscall() 异步执行
  - B: `os/src/arch/la64/trap/mod.rs:64-82` — trap_handler 同步调用 syscall()
- **参数提取/用户指针处理**
  - A: `hal/src/component/trap/riscv64/mod.rs:60-67` — TrapContextHal trait 抽象寄存器读取
  - B: `os/src/syscall/mod.rs:373-380` — 裸指针 a0 as const u8 透传

### 3.9 网络：10%

**对照判断：**独立设计。 双方模块职责存在对应关系，但描述中的接口和实现组织重合较少。

#### 相似点

- **实现位置与职责对照**
  - A: `os/src/net/chronix_tcp_socket.rs:180-180` — 用户态 TCP socket 封装，持有状态、handle、端点信息和各种标志位，通过 get_or_alloc_handle 惰性分配后端 handle。
  - B: `os/src/net/tcp.rs:100-100` — 用户态 TCP socket 封装，用 AtomicU8 维护状态机，UnsafeCell 存储 smoltcp（面向嵌入式环境的轻量网络协议栈） handle 和端点地址。
- **实现位置与职责对照**
  - A: `os/src/net/mod.rs:172-173` — 临时端口范围硬编码为 0xc000-0xffff，未参数化，修改后需全局搜索。
  - B: `os/src/net/mod.rs:82-87` — IP 地址和网关硬编码为 QEMU 和开发板的固定配置，跨平台部署需要修改源码重新编译。
- **实现位置与职责对照**
  - A: `os/src/net/chronix_tcp_socket.rs:521-528` — send 后周期性 yield 和反压 yield 是单核协作式调度下防止发送端饿死接收端的核心机制，直接解决 TCP 回环死锁。
  - B: `os/src/net/tcp.rs:343-348` — 根据目标地址是否为 127.0.0.1 选择回环接口或物理网卡，实现多网卡路由选择。

### 3.10 hal：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**hal 是 CINPHAL 内核的硬件抽象层，通过 trait 体系（ConstantsHal、FrameAllocatorHal、TrapTypeHal、InstructionHal 等）将架构相关细节（riscv64/loongarch64）与内核主体解耦。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T202612332999758-3324` | B：人工指定历史候选 `T202510213995926-2475` |
| --- | --- | --- |
| 技术家族 | rcore-rust | rcore-rust |
| 系统调用数量 | 259 | 262 |
| 描述模块 | 10 | 9 |
