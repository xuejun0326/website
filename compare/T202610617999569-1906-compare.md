# T202610617999569-1906 与人工指定历史候选 T202510003995291-2331 对比分析报告

- A（目标作品）：`T202610617999569-1906`
- B（人工指定历史候选）：`T202510003995291-2331`
- 候选身份：年份=unknown、学校=unknown、队伍=unknown
- 候选身份来源：`metadata_unavailable`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **48.2%**，对应“中度相似”。

模块级对照覆盖 **10** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | 系统调用层 | 75% | 做法接近 |
| 2 | 启动流程 | 70% | 做法接近 |
| 3 | 网络 | 65% | 目标相同、实现路径不同 |
| 4 | 文件系统 | 60% | 目标相同、实现路径不同 |
| 5 | 进程间通信 | 60% | 目标相同、实现路径不同 |
| 6 | 驱动框架 | 60% | 目标相同、实现路径不同 |
| 7 | 内存管理 | 55% | 目标相同、实现路径不同 |
| 8 | 进程与任务调度 | 55% | 目标相同、实现路径不同 |
| 9 | 信号机制 | 15% | 独立设计 |
| 10 | modules / lwext4 / rust | 0% | 仅历史候选实现 |

## 三、逐模块源码证据

### 3.1 系统调用层：75%

**对照判断：**做法接近。 两仓均以 handle_syscall 统一分发且覆盖大量系统调用，核心机制一致；但非法syscall处理和指针安全策略有差异，拉低相似度。

#### 相似点

- **统一分发入口**
  - A: `kernel/src/syscall/mod.rs:33-36` — handle_syscall 唯一入口，Sysno 枚举 match 分发
  - B: `src/syscall.rs:29-34` — handle_syscall 注册分发，Sysno::new 映射
- **用户指针安全封装**
  - A: `kernel/src/syscall/task/wait.rs:382-389` — waitpid 用 VmMutPtr 安全写退出状态
  - B: `api/src/ptr.rs:141-144` — UserPtr 提供 get_as_array 边界检查

#### 主要差异

- **非法 syscall 处理策略**
  - A: `kernel/src/syscall/mod.rs:33-36` — 非法系统调用号返回 ENOSYS
  - B: `src/syscall.rs:29-34` — 非法号 unwrap_or(memfd_secret) 吞错
- **用户指针使用一致性**
  - A: `kernel/src/syscall/time.rs:166-184` — clock_gettime 全面经 VmPtr 写用户空间
  - B: `api/src/imp/net/socket.rs:300-340` — socket 层裸指针构造切片，仅查 null

### 3.2 启动流程：70%

**对照判断：**做法接近。 两者均基于 axruntime 与 main 入口，构造用户程序并加载 ELF 建任务；差异在初始化分层与多模式支持。

#### 相似点

- **均基于 axruntime 引导，main 为入口**
  - A: `src/main.rs:41-58` — no_mangle main 作为 Rust 入口，调用内核 init
  - B: `src/main.rs:27-34` — main 在 axruntime 初始化后执行并启动用户程序
- **构造 busybox shell 启动命令**
  - A: `src/main.rs:9-21` — 编译期 feature 决定 CMDLINE，含 busybox 路径
  - B: `src/main.rs:27-34` — 构造 busybox shell 启动命令
- **创建用户地址空间并加载 ELF 生成任务**
  - A: `kernel/src/entry.rs:91-105` — 创建地址空间、拷贝内核映射、加载 ELF
  - B: `core/src/entry.rs:12-21` — run_user_app 建空地址空间、拷贝内核映射、加载 ELF

#### 主要差异

- **A 支持 feature 多模式与路径回退，B 固定**
  - A: `src/main.rs:9-21` — feature 选择 LTP/shell/init 脚本定制启动
  - B: `src/main.rs:27-34` — 仅构造固定 busybox 启动命令

### 3.3 网络：65%

**对照判断：**目标相同、实现路径不同。 核心机制一致（axnet+FileLike+fd表），但B仅支持TCP/UDP，选项、非阻塞等实现深度不足。

#### 相似点

- **都基于axnet协议栈**
  - A: `kernel/src/syscall/net/socket.rs:37-62` — sys_socket按domain/type分派到axnet的Tcp/Udp/Unix
  - B: `api/src/imp/net/socket.rs:29-32` — Socket枚举用Mutex包裹axnet的TcpSocket/UdpSocket
- **通过FileLike接入fd表**
  - A: `kernel/src/file/net.rs:15-40` — Socket实现FileLike，复用内核fd表
  - B: `api/src/imp/net/socket.rs:187-190` — Socket实现FileLike，接入通用fd表

#### 主要差异

- **UNIX socket支持差异**
  - A: `kernel/src/syscall/net/socket.rs:37-62` — AF_UNIX支持STREAM/SEQPACKET/DGRAM三种类型
  - B: `api/src/imp/net/socket.rs:29-32` — Socket枚举仅Udp/Tcp，无Unix变体
- **setsockopt实现深度差异**
  - A: `kernel/src/syscall/net/opt.rs:190-190` — 支持SO_ATTACH_BPF和多播组加入/离开
  - B: `api/src/imp/net/socket.rs:160-167` — setsockopt对UDP/TCP均为桩实现
- **非阻塞与CLOEXEC标志处理**
  - A: `kernel/src/syscall/net/socket.rs:37-62` — sys_socket处理NONBLOCK/CLOEXEC标志
  - B: `api/src/imp/net/socket.rs:287-290` — SOCK_NONBLOCK和SOCK_CLOEXEC处理被注释

### 3.4 文件系统：60%

**对照判断：**目标相同、实现路径不同。 核心抽象（FileLike、FlattenObjects fd 表）一致，但底层 VFS（虚拟文件系统，即统一不同文件系统的接口层） 来源和伪文件系统实现差异明显，故中等相似。

#### 相似点

- **统一 FileLike trait 抽象**
  - A: `kernel/src/file/mod.rs:150` — FileLike 提供 read/write/stat 等默认实现
  - B: `api/src/core/fs/fd.rs:22-33` — FileLike 是 fd 表项统一抽象，支持 downcast
- **FlattenObjects 实现 fd 表**
  - A: `kernel/src/file/mod.rs:363-366` — FD_TABLE 基于 FlattenObjects 稀疏数组
  - B: `api/src/core/fs/fd.rs:85-95` — FdTable 内部用 FlattenObjects 存储 fd

#### 主要差异

- **底层 VFS 来源不同**
  - A: `kernel/src/file/fs.rs:170` — File 为 axfs::File 包装，依赖 ArceOS
  - B: `modules/vfs/src/fs.rs:24-32` — 自研 FilesystemOps trait 定义 VFS 接口
- **伪文件系统实现机制不同**
  - A: `kernel/src/pseudofs/dir.rs:89-93` — ChainedDirOps 链式组合目录操作
  - B: `api/src/core/fs/pseudo/dynamic.rs:39-44` — DynamicFs 用 Slab 分配 inode 动态生成目录

### 3.5 进程间通信：60%

**对照判断：**目标相同、实现路径不同。 共享 System V（一组经典 Unix 系统接口约定） 共享内存与 futex（快速用户态互斥量，即用户态原子操作配合内核等待唤醒的同步机制） 机制，但权限检查、IPC_STAT、futex 跨进程能力差异显著。

#### 相似点

- **共享内存系统调用接口一致**
  - A: `kernel/src/syscall/ipc/shm.rs:380-380` — sys_shmget 创建/获取共享内存段
  - B: `api/src/interface/mm/shm.rs:26-26` — sys_shmget 类似 System V 语义
- **共享内存生命周期用 Arc 管理**
  - A: `kernel/src/syscall/ipc/shm.rs:382-395` — IPC_RMID 后仍有 attach 则延迟释放
  - B: `core/src/shared_memory.rs:1-74` — Arc 引用计数，Drop 归还页分配器
- **futex 以地址为键挂等待队列**
  - A: `kernel/src/task/futex.rs:213-218` — FutexKey 映射为 usize 哈希键
  - B: `core/src/process.rs:39-40` — 进程级 BTreeMap<usize, Arc<WaitQueue>>

#### 主要差异

- **共享内存权限检查差异**
  - A: `kernel/src/syscall/ipc/mod.rs:65-78` — owner/group/other 权限检查，root 放行
  - B: `api/src/interface/mm/shm.rs:29-30` — shmget 跳过权限检查，TODO
- **IPC_STAT 支持差异**
  - A: `kernel/src/syscall/ipc/shm.rs:555-559` — IPC_STAT 分支检查读权限位
  - B: `api/src/interface/mm/shm.rs:119-160` — IPC_STAT 被注释，返回 ENOSYS
- **futex 跨进程能力差异**
  - A: `kernel/src/task/futex.rs:187-206` — 共享内存 futex 按物理页标识对齐等待队列
  - B: `core/src/process.rs:39-40` — futex_table 挂在进程上，仅本进程可见

### 3.6 驱动框架：60%

**对照判断：**目标相同、实现路径不同。 共享 DeviceOps/Device 抽象和 mmap（内存映射，即把文件或匿名内存映射进进程地址空间） 物理映射，但 A 依赖 ArceOS 生态、支持 CoW（写时复制，即共享内存页只在写入时复制）/缓存映射，B 自研、仅物理映射且设备集不同。

#### 相似点

- **DeviceOps trait 统一设备操作**
  - A: `kernel/src/pseudofs/device.rs:28-47` — DeviceOps 定义 read_at/write_at/ioctl/mmap/as_pollable 等接口
  - B: `api/src/core/fs/pseudo/file.rs:149-162` — DeviceOps 定义 read_at/write_at/get_device_mem/ioctl，默认值返回错误
- **设备节点接入 VFS 文件系统**
  - A: `kernel/src/pseudofs/device.rs:63-111` — Device 组合 SimpleFsNode 与 DeviceOps，通过 inherit_methods 委托
  - B: `api/src/core/fs/pseudo/file.rs:177-194` — Device 包装 DynamicNode 与 DeviceOps，设置 raw_device 元数据
- **mmap 直映射设备物理内存**
  - A: `kernel/src/syscall/mm/mmap.rs:496-523` — sys_mmap downcast 到 Device，按 DeviceMmap 构造线性映射 Backend
  - B: `api/src/imp/mm/mmap.rs:154-180` — try_get_device_memory 后调 map_linear，剩余部分补零

#### 主要差异

- **底层驱动依赖来源不同**
  - A: `kernel/src/pseudofs/dev/event.rs:5-7` — 直接使用 axdriver::prelude 中的 AxInputDevice 等类型
  - B: `api/src/core/fs/imp/dev/framebuffer.rs:97` — FrameBuffer 自研实现，无外部驱动框架依赖
- **设备 mmap 语义丰富度不同**
  - A: `kernel/src/pseudofs/device.rs:16-21` — DeviceMmap 枚举支持物理、CoW 只读、缓存文件三种语义
  - B: `api/src/core/fs/pseudo/file.rs:146` — DeviceMem 仅含 physical_addr 与 length，只支持物理映射

### 3.7 内存管理：55%

**对照判断：**目标相同、实现路径不同。 共有 AddrSpace 抽象、缺页/mmap 接口，但后端模型、TLB 刷新及 Linux 语义完整度差异大。

#### 相似点

- **AddrSpace 核心抽象**
  - A: `kernel/src/mm/aspace/mod.rs:200` — AddrSpace 组合 VirtAddrRange、MemorySet 和 PageTable
  - B: `arceos/modules/axmm/src/aspace.rs:19-23` — AddrSpace 由 va_range、MemorySet<Backend> 和 PageTable 组成
- **缺页处理与访问标志**
  - A: `kernel/src/mm/access.rs:29-38` — access_user_memory 设置线程标志，缺页转发给地址空间
  - B: `core/src/mm.rs:214-225` — per-CPU 标志置位，缺页处理据此区分内核访问
- **mmap 系统调用**
  - A: `kernel/src/syscall/mm/mmap.rs:346` — sys_mmap 根据映射类型选择 Backend
  - B: `api/src/imp/mm/mmap.rs:89` — sys_mmap 支持匿名/文件/设备映射

#### 主要差异

- **TLB 刷新策略不同**
  - A: `kernel/src/mm/tlb.rs:30` — 按架构分派，RISC-V 用 SBI RFENCE 定向刷 TLB
  - B: `api/src/imp/mm/mmap.rs:222-231` — munmap 后显式刷新整个 TLB，mmap/mprotect 依赖按页刷新
- **mmap 语义支持程度不同**
  - A: `kernel/src/syscall/mm/mmap.rs:561-568` — 支持 GROWSDOWN 区域，缺页时动态扩展栈
  - B: `api/src/imp/mm/mmap.rs:89-106` — 仅检查 PRIVATE/SHARED 互斥，MAP_FIXED_NOREPLACE 不查重叠
- **文件/共享映射能力不同**
  - A: `kernel/src/mm/aspace/backend/mod.rs:239-254` — Backend 枚举包含 FileBackend、CowBackend 等四种后端
  - B: `api/src/imp/mm/mmap.rs:202-216` — 文件映射的 PROT_WRITE 未实现，数据不会写回文件

### 3.8 进程与任务调度：55%

**对照判断：**目标相同、实现路径不同。 共同基于 axtask 扩展进程模型，但 A 双层+外部 starry_process，B 四层自研进程组/会话，execve 策略差异明显。

#### 相似点

- **基于 axtask 扩展用户任务抽象**
  - A: `kernel/src/task/mod.rs:127-139` — Thread 为 axtask 用户态扩展，经 TaskExt 关联
  - B: `core/src/task.rs:19-26` — TaskExt 绑定 axtask 与 Thread/ThreadData
- **ProcessData 聚合进程级资源**
  - A: `kernel/src/task/mod.rs:285-294` — 聚合进程、信号、命名空间、exec 锁等
  - B: `core/src/process.rs:15` — 含地址空间、信号管理器、futex 表等
- **execve 对多线程语义均简化**
  - A: `kernel/src/syscall/task/execve.rs:273-290` — 非 leader 线程 exec 返回 WouldBlock
  - B: `api/src/imp/task/execve.rs:22-26` — 多线程下 execve 直接报错

#### 主要差异

- **进程层次模型不同**
  - A: `kernel/src/task/mod.rs:85` — Thread/ProcessData 双层，无会话进程组抽象
  - B: `process/src/lib.rs:1-5` — Session→ProcessGroup→Process→Thread 四层
- **execve 提交策略不同**
  - A: `kernel/src/syscall/task/execve.rs:273-290` — 先加载镜像后加锁提交，ExecStateGuard 回滚
  - B: `api/src/imp/task/execve.rs:22-26` — 多线程直接报错，无两阶段机制

### 3.9 信号机制：15%

**对照判断：**独立设计。 双方模块职责存在对应关系，但描述中的接口和实现组织重合较少。

#### 相似点

- **实现位置与职责对照**
  - A: `kernel/src/syscall/signal.rs:69-69` — 设置或查询信号处理动作，拒绝 SIGKILL/SIGSTOP 的修改。
  - B: `api/src/imp/task/signal.rs:120-120` — 设置或查询信号处理动作，拒绝 SIGKILL/SIGSTOP。
- **实现位置与职责对照**
  - A: `kernel/src/syscall/signal.rs:36-36` — 修改当前线程的信号屏蔽字，支持 SIG_BLOCK/SIG_UNBLOCK/SIG_SETMASK 三种操作。
  - B: `api/src/imp/task/signal.rs:90-100` — sys_rt_sigprocmask 通过 with_blocked_mut 闭包原子操作线程级屏蔽字，支持 SIG_BLOCK/SIG_UNBLOCK/SIG_SETMASK 三种模式。
- **实现位置与职责对照**
  - A: `kernel/src/task/signal.rs:96-96` — 向指定线程发送信号，处理 SIGCONT/SIGKILL 的 wait_status 更新和 ptrace 协作。
  - B: `api/src/imp/task/signal.rs:323-332` — sys_rt_sigsuspend 先移除 SIGKILL 和 SIGSTOP（不可屏蔽信号），再设置新屏蔽字并循环检查信号，通过忙等实现挂起语义。

### 3.10 modules / lwext4 / rust：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**该模块将 C 语言实现的 lwext4 (ext2/ext3/ext4 文件系统库) 通过 bindgen 生成的 FFI 绑定封装为 Rust 接口，核心抽象是 Ext4BlockDevice（块设备适配层）与 Ext4Filesystem（文件系统操作入口），并配套 InodeRef/DirReader/FileAttr 等 inode 级封装。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T202610617999569-1906` | B：人工指定历史候选 `T202510003995291-2331` |
| --- | --- | --- |
| 技术家族 | arceos-starry | arceos-starry |
| 系统调用数量 | 267 | 212 |
| 描述模块 | 9 | 10 |
