# T202510008995695-2720 ↔ nonix 对比分析报告

> **生成时间**: 2026-06-18 04:46 UTC
> **对比方向**: 以 **T202510008995695-2720** 为基准 (A), 分析 **nonix** (B) 的差异
> **运行时长**: 496.71s · prompt=39275 · completion=49136 · reasoning=45441
> **综合相似度**: **0.3558** (低度相似)

## 一、总览

| 维度 | A: T202510008995695-2720 | B: nonix |
|---|---|---|
| 家族 | `rcore-tutorial` | `rcore-tutorial` |
| Cargo 形态 | 单 crate | workspace · 2 成员 |
| 文件数 | 243 | 205 |
| 代码行数 | 64945 | 24240 |
| syscall 数 | 114 | 82 |
| 启动方式 | `global_asm` | `polyhal` |
| trap handlers | — | — |
| 已实现模块 | 启动流程 / 内存管理 / 进程与任务调度 / 文件系统 / 信号机制 / 网络 / 驱动框架 / 系统调用层 | 启动流程 / 内存管理 / 进程与任务调度 / 文件系统 / 信号机制 / 驱动框架 / 系统调用层 |
| 未实现模块 | 进程间通信 | 进程间通信 / 网络 |

## 二、结构差异

**目录 Jaccard**: 0.0342

| 仅 A 有 | 共有 | 仅 B 有 |
|---|---|---|
| `docs` | `os` | `bootloader` |
| `docs/final` | `os/src` | `doc` |
| `docs/img` | `user` | `lwext4_rust` |
| `docs/prel` | `user/src` | `lwext4_rust/c` |
| `docs/site` | — | `lwext4_rust/doc` |
| `os/cargo` | — | `lwext4_rust/examples` |
| `os/libs` | — | `lwext4_rust/riscv64-fs` |
| `os/vendor` | — | `lwext4_rust/src` |
| `user/cargo` | — | `patch` |
| — | — | `patch/cty` |
| — | — | `patch/polyhal` |
| — | — | `patch/virtio-drivers` |
| — | — | `user/test` |
| — | — | `vendor` |
| — | — | `vendor/aarch64-cpu` |

## 三、依赖差异

- A 总依赖数: **14**
- B 总依赖数: **17**
- 交集: **10**
- 仅 A 有: 4 项
- 仅 B 有: 7 项
- **依赖 Jaccard**: **0.4762**

### 仅 A 有的代表性依赖 (前 15)

- `bit_field`
- `loongarch64`
- `riscv`
- `smoltcp`

### 仅 B 有的代表性依赖 (前 15)

- `cfg-if`
- `fdt`
- `lazyinit`
- `polyhal`
- `polyhal-boot`
- `polyhal-trap`
- `zerocopy`

## 四、syscall 差异

- A 实现 syscall 数: **113**
- B 实现 syscall 数: **77**
- 共同实现: **70**
- **syscall Jaccard**: **0.5833**

### 4.1 仅 A 实现的 syscall (43 个)

`accept`, `bind`, `condvar_create`, `condvar_signal`, `condvar_wait`, `connect`, `enable_deadlock_detect`, `futex`, `get_mempolicy`, `get_robust_list`, `getsockname`, `getsockopt`  
`listen`, `lsm_set_self_attr`, `madvise`, `mail_read`, `mail_write`, `membarrier`, `mseal`, `msync`, `mutex_create`, `mutex_lock`, `mutex_unlock`, `recvfrom`  
`sched_getaffinity`, `semaphore_create`, `semaphore_down`, `semaphore_up`, `sendfile`, `sendto`, `setitimer`, `setpriority`, `setsid`, `setsockopt`, `socket`, `socketpair`  
`spawn`, `sync`, `sysinfo`, `tgkill`, `timerfd_gettime64`, `tkill`, `umask`

### 4.2 仅 B 实现的 syscall (7 个)

`copy_file_range`, `getpgid`, `io_setup`, `rt_sigsuspend`, `setpgid`, `shutdown`, `splice`

## 五、函数签名 Jaccard

- A 公开函数签名数 (`pub fn`): **4500**
- B 公开函数签名数: **6015**
- 完全相同的签名: **2397**
- **函数签名 Jaccard**: **0.2953**

### 5.1 仅 A 暴露的接口样本 (前 10)

- `sys_mutex_create(bool) -> isize`
- `get_ref(SELF) -> &D`
- `set_bar_64(SELF,DeviceFunction,u8,u64) -> ()`
- `set_psh(SELF,bool) -> ()`
- `get(SELF,Key) -> Option<&T>`
- `insert(SELF,String,usize) -> ()`
- `igmp_leave_packet(SELF,Ipv4Address) -> Option<Packet<>>`
- `finish(SELF) -> ()`
- `make_pipe() -> (Arc<Pipe>,Arc<Pipe>)`
- `syscall(usize,[usize;3]) -> isize`

### 5.2 仅 B 暴露的接口样本 (前 10)

- `set_addr(SELF,PhysAddr,PageTableFlags) -> ()`
- `align_down_to_base_page(SELF) -> Self`
- `create_cache(SELF) -> Cache`
- `matches_byte(SELF,u8) -> Option<StateID>`
- `highest_cos(SELF) -> u16`
- `or_insert_with_key(SELF,F) -> &mutV`
- `set_condition(SELF,DebugAddressRegisterNumber,BreakpointCondition) -> ()`
- `optimize_for_prefix_by_preference(SELF) -> ()`
- `clocks(SELF) -> implIterator<Item=ClockRef<>>+`
- `raw(R) -> ()`

## 六、调用图差异 (轻量版)

> 第一版用集合层数字 + 高频函数名列表把『两个内核调用模式有多接近』讲清楚, 不画 mermaid (后续优化项)。

### 6.1 调用图统计

| 维度 | A | B |
|---|---|---|
| 节点数 (函数) | 7919 | 8601 |
| 边数 (调用关系) | 28249 | 29493 |
| 平均出度 | 5.06 | 5.03 |

### 6.2 节点 Jaccard: **0.284**

### 6.3 边 Jaccard: **0.2353**

### 6.4 综合调用图相似度 = 0.5·node + 0.5·edge = **0.2596**

### 6.5 高频被调函数 Top 10

**A (T202510008995695-2720)**:

- `new` — 3345 次
- `Ok` — 1762 次
- `field` — 1640 次
- `unwrap` — 1597 次
- `Some` — 1450 次
- `clone` — 1359 次
- `len` — 1117 次
- `parse` — 957 次
- `Err` — 875 次
- `iter` — 729 次

**B (nonix)**:

- `new` — 3375 次
- `field` — 2222 次
- `Ok` — 2125 次
- `Some` — 1727 次
- `clone` — 1339 次
- `Err` — 1180 次
- `unwrap` — 1147 次
- `parse` — 998 次
- `map` — 936 次
- `len` — 875 次

## 七、模块级语义对照

> 每条差异点的 file:line 会进入 §九 的验证透明表逐条核对。

### 7.1 启动 (`boot`)

**对照判定**: 实现路径分化

**A 仓做法**: 通过global_asm!内嵌双架构汇编入口，以Rust main为初始化编排点，串行初始化全部子系统，最后开启时钟中断。独特扩展：双架构支持、Rust BSS清零、新增网络和信号初始化。

**B 仓做法**: 以main(hartid)为入口，通过polyhal_boot宏从汇编跳转。使用PageAllocImpl适配器连接polyhal和内核帧分配器。启动序列按依赖排序：堆、日志、polyhal公共层、物理帧、文件系统、任务，最终调用task::run_tasks()。

**关键差异点**:

- **多核启动支持** (独立)
  - A: `os/src/boot.rs:9-28` — RISC-V入口中CPU ID读取被注释，导致多核场景未定义
  - B: `os/src/main.rs:68-71` — 单核守卫，只处理hart 0，其他hart返回
- **初始化序列中网络和信号模块** (新增)
  - A: `os/src/main.rs:85-108` — 初始化链包含网络和信号模块初始化
  - B: `os/src/main.rs:84-89` — 初始化序列仅到文件系统和任务，无网络和信号
- **平台初始化方式** (替换)
  - A: `os/src/boot.rs:37-68` — LoongArch特有DMW窗口和csrwr屏障操作
  - B: `os/src/main.rs:76-83` — 通过polyhal::common::init进行公共初始化，注入PageAllocImpl

### 7.2 内存管理 (`mm`)

**对照判定**: 独立设计

**A 仓做法**: 基于rCore-Tutorial的三层地址空间模型，支持SV39/LA双架构，包含页表、MapArea、MemorySet，扩展了动态链接器、文件mmap回写、System V共享内存等。

**B 仓做法**: 未提供详细描述

(无显著差异)

### 7.3 进程/任务 (`task`)

**对照判定**: 独立设计

**A 仓做法**: (LLM 未给出)

**B 仓做法**: 本模块以 TaskControlBlock (TCB) 为核心抽象，通过 UPSafeCell 封装可变内部状态，调度采用基于 VecDeque 的简单 FIFO 就绪队列，并新增了信号处理、clone 系统调用、exec 辅

(无显著差异)

### 7.4 文件系统 (`fs`)

**对照判定**: 实现路径分化

**A 仓做法**: 本模块采用「双轨 VFS」架构: 常规文件走 `Inode` trait (由 ext4 后端 `Ext4Inode` 实现), 抽象文件 (设备/管道/stdio) 走 `File` trait, 二者通过 `FileClass` 枚举统一收口.

**B 仓做法**: 该模块以统一的 `File` trait 为抽象核心, 将真实文件 (ext4, 通过 `OSInode` 封装) 与虚拟文件 (管道、`/proc` 等, 通过直接实现 `File` 的类型) 纳入同一框架.

**关键差异点**:

- **双trait (Inode+File) vs 统一File抽象** (独立)
  - A: `os/src/fs/ext4_lw/inode.rs:19-26` — Ext4Inode 实现 Inode trait, 体现双轨设计
  - B: `os/src/fs/inode.rs:30-43` — OSInode 直接持有 Ext4Inode, 无独立 Inode trait
- **符号链接递归解析 vs 未实现** (新增)
  - A: `os/src/fs/ext4_lw/inode.rs:153-192` — 符号链接递归解析与循环检测
  - B: `os/src/fs/ext4_lw/inode.rs:172-201` — read_all 对非普通文件 panic, 符号链接未支持
- **设备注册表与硬编码路由 vs 纯虚拟文件** (独立)
  - A: `os/src/fs/devfs.rs:27-28` — 全局设备注册表 BTreeMap
  - B: `os/src/fs/inode.rs:193-250` — open 仅检查虚拟文件查找, 无设备注册表
- **动态模拟 procfs vs 静态硬编码文件** (独立)
  - A: `os/src/fs/mod.rs:245-375` — 在 ext4 上模拟 procfs 并动态管理进程目录
  - B: `os/src/fs/usedfiles.rs:5-43` — 硬编码 /proc/meminfo 内容, 无动态管理

### 7.5 信号 (`signal`)

**对照判定**: 实现路径分化

**A 仓做法**: 类 Linux 信号机制：33信号位集、SigTable+KSigAction，通过setup_frame/restore_frame递送，支持SA_SIGINFO/SA_RESTART/SA_RESTORER及tgkill，仅riscv64。

**B 仓做法**: 双层抽象：用户SigAction，内核KSigAction附加customed；SignalFlags位集，default_op分配5类默认行为；SigTable UPSafeCell保护。有POSIX骨架但SA_*/Stop未实现，无排队。

**关键差异点**:

- **SA_SIGINFO 标志支持** (新增)
  - A: `os/src/signal/mod.rs:117-145` — 实现 SA_SIGINFO 帧布局，构造 UserContext/SigInfo，设置 a1/a2
  - B: `os/src/signal/sigflags.rs:144-167` — SA_* 标志常量定义但全部未实现
- **SA_RESTART 标志支持** (新增)
  - A: `os/src/signal/mod.rs:93-114` — 实现 SA_RESTART 系统调用重启逻辑，读取 scause
  - B: `os/src/signal/sigflags.rs:144-167` — SA_* 标志常量定义但全部未实现
- **信号数量范围** (新增)
  - A: `os/src/signal/signal.rs:4-37` — 定义 33 种信号（包括实时信号 SIGRT_1）
  - B: `os/src/signal/sigflags.rs:37` — SignalFlags 为 32 位，限制最大信号 31

### 7.6 IPC (`ipc`)

**对照判定**: 两边都未实现

两仓均未实现该模块。

### 7.7 网络 (`net`)

**对照判定**: 仅 A 实现

**A 仓做法**: 基于 smoltcp 实现 POSIX 兼容的 TCP/UDP 套接字，仅驱动单 loopback 设备；通过无锁状态机和协同调度实现阻塞 I/O。

**B 仓**: 未实现该模块

(无显著差异)

### 7.8 驱动 (`drivers`)

**对照判定**: 实现路径分化

**A 仓做法**: 两级 trait（BaseDriver/BlockDriver）驱动的块设备框架，支持 MMIO/PCI 虚拟化设备，含网络驱动，编译期 target_arch 派发。

**B 仓做法**: 基于 BaseDriver/BlockDriver 的块设备驱动，通过 VirtioTransportImpl 枚举统一架构传输层，提供游标式 Disk 访问，无网络支持。

**关键差异点**:

- **BaseDriver 接口差异** (独立)
  - A: `os/src/drivers/device.rs:1-46` — BaseDriver 包含 device_name 和 device_type
  - B: `os/src/drivers/device.rs:3-6` — BaseDriver 仅含 device_name
- **传输层抽象方式差异** (替换)
  - A: `os/src/drivers/mod.rs:45-55` — 按 target_arch 编译期派发 MMIO/PCI
  - B: `os/src/drivers/tran_impl.rs:9-14` — VirtioTransportImpl 枚举统一传输层
- **PCI 枚举代码重复差异** (优化)
  - A: `os/src/drivers/virtio/blk.rs:160-210` — blk.rs 中 PCI 枚举，与 net.rs 重复
  - B: `os/src/drivers/virtio_blk.rs:117-150` — virtio_blk.rs 中 PCI 枚举，仅一处
- **share 实现方式差异** (替换)
  - A: `os/src/drivers/virtio/mod.rs:27-80` — 通过 KERNEL_SPACE 页表翻译
  - B: `os/src/drivers/virtio_blk.rs:289-296` — 直接使用线性映射反推物理地址
- **dma_alloc 连续性检查差异** (新增)
  - A: `os/src/drivers/virtio/mod.rs:28-28` — dma_alloc 分配连续页，无连续性断言
  - B: `os/src/drivers/virtio_blk.rs:230-244` — dma_alloc 断言物理页连续，否则 panic

### 7.9 系统调用 (`syscall`)

**对照判定**: 实现路径分化

**A 仓做法**: rCore-Tutorial 基线: 约20个系统调用, 简单实现, 返回 isize。

**B 仓做法**: 以 syscall() 为入口, 平坦 match 分派约80个 syscall, 返回 SyscallRet, 使用 UserBuffer 翻译用户态指针。

(无显著差异)


## 八、B 相对 A 的关键差异

**总评**: B (nonix) 与 A 同属 rCore-Tutorial 家族，但 B 走向轻量级实验原型：采用统一 File trait 替代双轨 VFS，引入 polyhal 硬件抽象但未完成，聚焦信号与任务子系统却大量留白。A 则追求功能广度，内置网络栈、双架构原生支持与更完整的 POSIX 信号，代码量达 B 的 2.7 倍。两者在文件系统、启动、信号设计上呈现明显分化。

**整体定位**: 同源分化 — 两仓共享起点但走向不同

### 8.1 双轨 VFS vs 统一 File trait

**类型**: 替换 · **显著度**: ★★★★☆

A 通过 FileClass 枚举分离 Inode（命名空间操作）与 File（读写），设备与管道可直接实现 File 而无需伪造 inode；B 则让所有对象（ext4、管道、proc）直接实现统一的 File trait，舍弃了 Inode 层。这是文件系统架构的根本分歧。

- A: `os/src/fs/mod.rs:85-98` — FileClass 枚举三变体，双轨 VFS 核心
- B: `os/src/fs/pipe.rs:271-393` — 管道实现 File trait，统一抽象

### 8.2 网络栈缺失

**类型**: 取消 · **显著度**: ★★★★★

A 基于 smoltcp 实现了 loopback TCP/UDP 网络栈，包含无锁状态机、accept 机制与协同阻塞 I/O；B 的启动流程未初始化网络，整个 net 模块为空白，syscall 也无网络相关。这是核心功能集的重大差异。

- A: `os/src/net/socket/mod.rs:460-478` — 仅初始化单 loopback 设备，但确有网络栈
- B: `os/src/main.rs:84-89` — 启动仅含文件系统与任务子系统，无网络初始化

### 8.3 信号动作的表示方法分化

**类型**: 独立 · **显著度**: ★★★☆☆

A 采用直接的 SigAction 函数指针表示信号处理，通过 SignalStack (trampoline) 实现用户态 handler；B 则设计 KSigAction 双层结构，附加 customed 标志以区分内核默认与用户设置，避免热路径条件判断，但 SA_* 标志未实现。两者独立演进。

- A: `os/src/signal/signal.rs:149-173` — 默认 SigAction 构造，直接映射函数指针
- B: `os/src/signal/sigact.rs:48-52` — KSigAction 包裹 SigAction 并附加 customed 标志

### 8.4 跨架构启动：内嵌汇编 vs polyhal 抽象

**类型**: 替换 · **显著度**: ★★★★☆

A 通过 global_asm! 内嵌 RV/LA 汇编入口，并以条件编译支持双架构；B 则利用 polyhal_boot 宏与 PageAllocImpl 适配器，试图将硬件差异封装在 polyhal 层。B 的抽象更现代但集成未完成，实际仍硬编码架构地址。

- A: `os/src/boot.rs:9-28` — RISC-V 入口汇编，global_asm! 内嵌
- B: `os/src/main.rs:76-83` — polyhal 公共层初始化，PageAlloc 适配器注入


## 九、综合相似度评分

### 9.1 各维度得分

| 维度 | 权重 | A↔B 得分 | 加权 |
|---|---|---|---|
| 函数签名 Jaccard | 0.30 | 0.2953 | 0.0886 |
| syscall Jaccard | 0.20 | 0.5833 | 0.1167 |
| 依赖 Jaccard | 0.20 | 0.4762 | 0.0952 |
| 调用图综合 | 0.20 | 0.2596 | 0.0519 |
| 目录 Jaccard | 0.10 | 0.0342 | 0.0034 |
| **合计** | **1.00** | — | **0.3558** |

### 9.2 等级判定

| 范围 | 等级 |
|---|---|
| ≥ 0.70 | 高度相似 |
| 0.40 - 0.70 | 中度相似 |
| 0.15 - 0.40 | 低度相似 |
| < 0.15 | 完全不同 |

**本次评级**: **低度相似** (0.3558)

### 9.3 公式

```text
overall = 0.30 × sig_jaccard
        + 0.20 × syscall_jaccard
        + 0.20 × deps_jaccard
        + 0.20 × callgraph_score (= 0.5 × node_jaccard + 0.5 × edge_jaccard)
        + 0.10 × dir_jaccard
```

## 十、附录: 警告与错误

*无警告或错误。*

---
*本报告由 oskag compare 自动生成, §七/§八 引用均经 verifier 二次校验, 不修饰失败。*