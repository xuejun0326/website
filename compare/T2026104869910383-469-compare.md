# T2026104869910383-469 与人工指定历史候选 T202510003995291-2331 对比分析报告

- A（目标作品）：`T2026104869910383-469`
- B（人工指定历史候选）：`T202510003995291-2331`
- 候选身份：年份=unknown、学校=unknown、队伍=unknown
- 候选身份来源：`metadata_unavailable`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **49.1%**，对应“中度相似”。

模块级对照覆盖 **10** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | 信号机制 | 88% | 做法接近 |
| 2 | 系统调用层 | 85% | 做法接近 |
| 3 | 进程与任务调度 | 78% | 做法接近 |
| 4 | 启动流程 | 75% | 做法接近 |
| 5 | 内存管理 | 65% | 目标相同、实现路径不同 |
| 6 | 文件系统 | 55% | 目标相同、实现路径不同 |
| 7 | 进程间通信 | 55% | 目标相同、实现路径不同 |
| 8 | 网络 | 17% | 独立设计 |
| 9 | 驱动框架 | 14% | 独立设计 |
| 10 | modules / lwext4 / rust | 0% | 仅历史候选实现 |

## 三、逐模块源码证据

### 3.1 信号机制：88%

**对照判断：**做法接近。 核心机制和接口高度一致，差异为 signalfd 缺失和 sigtimedwait 返回值错误。

#### 相似点

- **双层信号管理结构**
  - A: `kernel/src/task/mod.rs:72-94` — 线程持有 ThreadSignalManager，进程持有 ProcessSignalManager
  - B: `core/src/process.rs:37-39` — ProcessData 持有进程级管理器，线程共享
- **返回用户态前检查信号**
  - A: `kernel/src/task/signal.rs:21-50` — check_signals 在返回用户态时检查并递送信号
  - B: `api/src/imp/task/signal.rs:25-29` — check_signals 在 POST_TRAP 回调中调用
- **拒绝捕获 SIGKILL/SIGSTOP**
  - A: `kernel/src/syscall/signal.rs:106-130` — rt_sigaction 拒绝设置不可捕获信号
  - B: `api/src/imp/task/signal.rs:120-120` — sys_rt_sigaction 拒绝 SIGKILL/SIGSTOP

#### 主要差异

- **signalfd 支持差异**
  - A: `kernel/src/file/signalfd.rs:1-16` — signalfd 通过文件接口消费信号
  - B: `src/syscall.rs:448-448` — signalfd4 未实现，返回 ENOSYS
- **sigtimedwait 返回值差异**
  - A: `kernel/src/syscall/signal.rs:327-348` — 返回实际信号编号，并特殊处理 SIGCHLD
  - B: `api/src/imp/task/signal.rs:312-318` — 成功时返回 0，不符合 Linux 语义

### 3.2 系统调用层：85%

**对照判断：**做法接近。 统一入口、arg0-5提取与负errno约定高度一致，差异仅在非法syscall号处理策略。

#### 相似点

- **统一入口与参数分发机制**
  - A: `kernel/src/syscall/mod.rs:79-88` — handle_syscall从UserContext取号，match分发至sys_函数
  - B: `src/syscall.rs:29-34` — handle_syscall经trap（陷入，即异常、中断或系统调用进入内核的统一路径）注册，Sysno枚举匹配调用实现
- **错误码统一转为负值返回**
  - A: `kernel/src/syscall/mod.rs:79-88` — Err转为负LinuxError code写入uctx.retval
  - B: `src/syscall.rs:29-34` — unwrap_or_else将err.code()取负返回用户态

#### 主要差异

- **非法syscall号处理策略**
  - A: `kernel/src/syscall/mod.rs:79-88` — 非法号返回-ENOSYS
  - B: `src/syscall.rs:29-34` — 非法号被静默替换为memfd_secret

### 3.3 进程与任务调度：78%

**对照判断：**做法接近。 双仓共享 Thread/ProcessData 双层抽象、axtask TaskExt、进程组/会话及弱引用防循环；差异在 execve 多线程与线程封装粒度，故给 78。

#### 相似点

- **Thread/ProcessData 双层次与 TaskExt 集成**
  - A: `kernel/src/task/mod.rs:72-89` — Thread 封装调度/信号/定时器，经 TaskExt 挂接 axtask
  - B: `core/src/task.rs:19-26` — TaskExt 将 axtask 调度实体与 POSIX（可移植操作系统接口，即 Unix 类系统通用接口规范） Thread/ProcessData 绑定
- **进程组/会话层次模型**
  - A: `kernel/src/task/ops.rs:48-48` — 注册任务到全局表时同时注册进程/进程组/会话
  - B: `process/src/lib.rs:1-5` — 声明 Session→ProcessGroup→Process→Thread 四层模型
- **弱引用避免循环引用**
  - A: `kernel/src/task/ops.rs:25-28` — 四个全局表用 WeakMap 弱引用，条目自动失效
  - B: `process/src/process.rs:11-20` — Process 用 Weak 指向父进程/进程组，避免循环引用

#### 主要差异

- **execve 多线程语义**
  - A: `kernel/src/task/mod.rs:233-240` — exec_exit 与 exit 分离，仅保留执行 exec 的线程
  - B: `api/src/imp/task/execve.rs:22-26` — 多线程进程下 execve 直接报错，未做去留处理
- **线程状态的封装粒度**
  - A: `kernel/src/task/mod.rs:72-89` — Thread 承载调度策略/信号/定时器等完整线程状态
  - B: `process/src/thread.rs:7-10` — Thread 仅含 tid 与 Process 弱引用，线程状态集中在 Process

### 3.4 启动流程：75%

**对照判断：**做法接近。 共同机制：先复制内核映射再加载 ELF、Rust 高层入口生成 PID（进程标识符，即系统用于区分进程的编号） 1；差异：shell 探测 vs 固定命令、屏障 vs 缺页处理。

#### 相似点

- **用户地址空间创建方式相同**
  - A: `kernel/src/entry.rs:144-151` — 复制内核映射后加载用户程序
  - B: `core/src/entry.rs:12-21` — 同样先复制内核映射
- **Rust 高层封装启动逻辑**
  - A: `kernel/src/entry.rs:153-162` — 构造任务上下文并 spawn PID 1
  - B: `core/src/entry.rs:12-21` — 创建任务并注册到调度器

#### 主要差异

- **shell 探测与固定命令**
  - A: `kernel/src/entry.rs:86-104` — 探测 TEST_RUNNER_CANDIDATES 列表
  - B: `src/main.rs:27-34` — 硬编码 /musl/busybox sh -c <testcase>
- **注册屏障与缺页处理器**
  - A: `kernel/src/entry.rs:116-127` — 注册 IPI 中断和 TLB 刷新屏障
  - B: `src/mm.rs:14-23` — 注册 PAGE_FAULT 陷阱处理器

### 3.5 内存管理：65%

**对照判断：**目标相同、实现路径不同。 核心抽象与缺页处理一致，但后端模型、预取、TLB刷新策略等实现路径差异明显，整体相似度中等。

#### 相似点

- **核心抽象一致**
  - A: `kernel/src/mm/aspace/mod.rs:21-27` — AddrSpace封装虚拟地址范围、MemorySet区域集合与PageTable
  - B: `arceos/modules/axmm/src/aspace.rs:19-23` — AddrSpace含va_range、MemorySet和PageTable
- **缺页处理机制**
  - A: `kernel/src/mm/aspace/mod.rs:383-389` — handle_page_fault实现fault-around预取和COW（写时复制，即共享内存页只在写入时复制）处理
  - B: `src/mm.rs:14-38` — 缺页处理先判断内核缺页，再委托AddrSpace补齐映射
- **mmap（内存映射，即把文件或匿名内存映射进进程地址空间）系统调用**
  - A: `kernel/src/syscall/mm/mmap.rs:115-122` — sys_mmap根据类型选择后端，处理MAP_FIXED
  - B: `api/src/imp/mm/mmap.rs:89-106` — sys_mmap检查标志，支持匿名/文件/设备映射

#### 主要差异

- **后端抽象设计**
  - A: `kernel/src/mm/aspace/backend/mod.rs:107-114` — 通过enum_dispatch实现Linear/Cow/Shared/File四后端
  - B: `core/src/mm.rs:72-89` — ELF段一次性分配物理页写入，无多后端抽象
- **缺页预取优化**
  - A: `kernel/src/mm/aspace/mod.rs:383-389` — 在64KiB窗口内fault-around预取减少缺页
  - B: `src/mm.rs:14-38` — 缺页处理仅委托AddrSpace，无预取机制
- **TLB刷新策略**
  - A: `kernel/src/mm/mod.rs:12-19` — 跨CPU TLB/指令缓存屏障通过IPI实现
  - B: `api/src/imp/mm/mmap.rs:222-231` — munmap后显式刷新整个TLB，其余依赖按页刷新

### 3.6 文件系统：55%

**对照判断：**目标相同、实现路径不同。 共有 FileLike/FlattenObjects fd 抽象与只读伪文件系统，但 VFS（虚拟文件系统，即统一不同文件系统的接口层） 分层、缓存与并发模型不同，整体实现路径分叉。

#### 相似点

- **FileLike trait 统一 fd 抽象**
  - A: `kernel/src/file/mod.rs:184-190` — A 将所有 fd 对象统一为 FileLike trait
  - B: `api/src/core/fs/fd.rs:22-33` — B 同样以 FileLike 作为 fd 表项统一抽象
- **fd 表使用 FlattenObjects 稀疏数组**
  - A: `kernel/src/file/mod.rs:239-245` — A 的 FileTable 用 FlattenObjects 存储 fd 条目
  - B: `api/src/core/fs/fd.rs:85-95` — B 的 FdTable 同样基于 FlattenObjects
- **伪文件系统只读，修改操作返回错误**
  - A: `kernel/src/pseudofs/dir.rs:190-197` — A 的 pseudofs create/link/unlink 均返回 OperationNotPermitted
  - B: `api/src/core/fs/pseudo/dynamic.rs:325-332` — B 的 DynamicDir 同类操作返回 EPERM

#### 主要差异

- **VFS 框架分层差异**
  - A: `kernel/src/file/mod.rs:184-190` — A 无独立 VFS 框架层，FileLike 直接承担 fd 对象抽象
  - B: `modules/vfs/src/fs.rs:24-32` — B 在底层额外定义 FilesystemOps trait，形成 VFS 框架
- **缓存机制不同**
  - A: `kernel/src/file/fs.rs:254-274` — A 提供 per-file KstatCache，seqlock 无锁读 stat
  - B: `modules/vfs/src/node/dir.rs:122-132` — B 在目录项 lookup 处用 BTreeMap 做缓存

### 3.7 进程间通信：55%

**对照判断：**目标相同、实现路径不同。 共享内存与 futex（快速用户态互斥量，即用户态原子操作配合内核等待唤醒的同步机制） 存在共同设计且命令集一致，但 SysV 覆盖范围、映射方式与 futex 作用域分歧明显，故评 55 分。

#### 相似点

- **共享内存全局管理器与 key 索引**
  - A: `kernel/src/syscall/ipc/shm.rs:295-308` — ShmManager 维护 key↔shmid 三重映射
  - B: `core/src/shared_memory.rs:28` — BTreeMap 按 key 索引 Arc<SharedMemory>
- **futex 快速用户态互斥命令集**
  - A: `kernel/src/task/futex.rs:33-43` — WaitQueue 支持 FUTEX_WAKE_BITSET，Waiter 弱引用
  - B: `api/src/imp/task/futex.rs:18-143` — 实现 WAIT/WAKE/REQUEUE/BITSET 命令
- **Arc 管理共享内存生命周期**
  - A: `kernel/src/syscall/ipc/shm.rs:100` — ShmInner 持有物理页 Arc<SharedPages>（共享物理页抽象）
  - B: `core/src/shared_memory.rs:6` — Drop 自动归还页分配器

#### 主要差异

- **SysV 消息队列/信号量 vs 管道**
  - A: `kernel/src/syscall/ipc/msg.rs:88` — MessageQueue 按类型组织消息
  - B: `api/src/core/file/pipe.rs:17` — PipeRingBuffer 实现字节流管道
- **futex 等待队列作用域**
  - A: `kernel/src/task/futex.rs:20` — WaitQueue 定义于 task 模块，未挂进程
  - B: `core/src/process.rs:39-40` — futex_table 为进程级 BTreeMap
- **共享内存页表映射方式**
  - A: `kernel/src/syscall/ipc/shm.rs:400` — 复用内存管理模块的 SharedPages 机制
  - B: `api/src/interface/mm/shm.rs:108-111` — virt_to_phys 转物理地址后线性映射

### 3.8 网络：17%

**对照判断：**独立设计。 双方模块职责存在对应关系，但描述中的接口和实现组织重合较少。

#### 相似点

- **实现位置与职责对照**
  - A: `kernel/src/syscall/net/socket.rs:26-26` — 创建套接字，按 domain/type 分发到 TCP/UDP/Unix/Vsock 实现，处理非阻塞和 CLOEXEC 标志。
  - B: `api/src/imp/net/socket.rs:278-278` — 创建 socket 并注册到 fd 表，返回 fd 号，支持 TCP/UDP。
- **实现位置与职责对照**
  - A: `kernel/src/file/net.rs:20-20` — 包装 axnet SocketInner 的 FileLike 适配器，实现 read/write/stat/from_fd 等文件接口。
  - B: `api/src/imp/net/socket.rs:187-190` — Socket 实现 FileLike trait，使 read/write 系统调用可直接作用于 socket，与普通文件描述符统一。
- **实现位置与职责对照**
  - A: `kernel/src/file/net.rs:65-72` — Socket::from_fd 从 FD 表取出文件对象并向下转型，非 socket 的 fd 返回 NotASocket 错误。
  - B: `api/src/imp/net/socket.rs:39-44` — from_fd 从 fd 表反查 Socket 对象，通过 downcast 确保类型正确，失败返回 EINVAL。

### 3.9 驱动框架：14%

**对照判断：**独立设计。 双方模块职责存在对应关系，但描述中的接口和实现组织重合较少。

#### 相似点

- **实现位置与职责对照**
  - A: `kernel/src/pseudofs/dev/fb.rs:84-84` — 获取显存信息并启动 60Hz 刷新任务，返回 FrameBuffer 设备实例。
  - B: `api/src/core/fs/imp/dev/framebuffer.rs:126-126` — 返回帧缓冲物理地址与长度，供 mmap 直映射。
- **实现位置与职责对照**
  - A: `kernel/src/pseudofs/device.rs:8-8` — 设备 mmap 行为枚举，区分不可映射/物理映射/只读 CoW/缓存文件四种模式。
  - B: `api/src/utils/dev.rs:5-5` — 从文件描述符反查 Device 节点，供 mmap 路径使用。
- **实现位置与职责对照**
  - A: `kernel/src/pseudofs/device.rs:24-24` — 设备操作 trait，定义 read_at/write_at/ioctl/mmap/poll 等接口，所有设备驱动必须实现。
  - B: `api/src/core/fs/pseudo/file.rs:149-149` — 设备驱动统一接口，含 read_at/write_at/get_device_mem/ioctl。

### 3.10 modules / lwext4 / rust：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**该模块将 C 语言实现的 lwext4 (ext2/ext3/ext4 文件系统库) 通过 bindgen 生成的 FFI 绑定封装为 Rust 接口，核心抽象是 Ext4BlockDevice（块设备适配层）与 Ext4Filesystem（文件系统操作入口），并配套 InodeRef/DirReader/FileAttr 等 inode 级封装。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T2026104869910383-469` | B：人工指定历史候选 `T202510003995291-2331` |
| --- | --- | --- |
| 技术家族 | arceos-starry | arceos-starry |
| 系统调用数量 | 244 | 212 |
| 描述模块 | 9 | 10 |
