# T2026104869911125-3219 与人工指定历史候选 T202410287992755-2176 对比分析报告

- A（目标作品）：`T2026104869911125-3219`
- B（人工指定历史候选）：`T202410287992755-2176`
- 候选身份：年份=unknown、学校=unknown、队伍=unknown
- 候选身份来源：`metadata_unavailable`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **0%**，对应“完全不同”。

模块级对照覆盖 **15** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | ext4文件系统 | 0% | 仅历史候选实现 |
| 2 | 公共头文件 | 0% | 仅历史候选实现 |
| 3 | 内存管理 | 0% | 仅历史候选实现 |
| 4 | 内核启动 | 0% | 仅历史候选实现 |
| 5 | 内核基础库 | 0% | 仅历史候选实现 |
| 6 | 内核核心功能 | 0% | 仅目标作品实现 |
| 7 | 同步与信号 | 0% | 仅历史候选实现 |
| 8 | 用户态支持 | 0% | 仅历史候选实现 |
| 9 | 用户态程序接口 | 0% | 仅目标作品实现 |
| 10 | 第三方基础库 | 0% | 仅目标作品实现 |
| 11 | 虚拟文件系统 | 0% | 仅历史候选实现 |
| 12 | 计分板生成工具 | 0% | 仅目标作品实现 |
| 13 | 设备驱动 | 0% | 仅历史候选实现 |
| 14 | 进程管理 | 0% | 仅历史候选实现 |
| 15 | 陷阱与系统调用 | 0% | 仅历史候选实现 |

## 三、逐模块源码证据

### 3.1 ext4文件系统：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**该模块以 lwext4 移植为核心，提供 ext4 文件系统的高层文件操作（ext4_fopen/fread/fwrite 等），并在此基础上构建了 VFS（虚拟文件系统，即统一不同文件系统的接口层） 层（VFSM/FileNode）与进程文件描述符管理（FileObjectManager）。

### 3.2 公共头文件：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**Include 目录是内核全部头文件的集合，核心抽象包括：RISC-V 架构寄存器/指令编码宏（Riscv.h/Riscv.hpp）、SV39 三级页表与虚拟内存空间管理（vmm.hpp）、进程与进程管理（Process.hpp）、ELF 解析（ParseELF.hpp）、lwext4 文件系统磁盘结构定义（ext4_types.h）以及通用树/队列容器（t。

### 3.3 内存管理：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**该模块实现了一个基于 RISC-V Sv39 的完整内存管理子系统，包含三层结构：PMM 物理页分配器（双向链表最优适配）、Slab 小对象分配器（按 64B/512B/1024B 分桶）、VMM 虚拟内存空间管理（VMR 区域链表 + 三级页表 + 缺页处理）。

### 3.4 内核启动：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**Kernel_Boot 模块由 Start.S 汇编入口与 main.cpp 的 C++ 主函数构成，负责从 OpenSBI 接管后完成页表建立、BSS 清零、中断/时钟/内存/磁盘/文件系统/进程管理器的初始化，并最终以 busybox 脚本方式驱动用户态测试。核心抽象是启动页表（SV39 大页映射）与 main() 中的顺序初始化管线。

### 3.5 内核基础库：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**该模块是内核自研的基础运行库，包含三部分：Kstring.cpp 提供无标准库依赖的字符串/内存操作（strlen/strcpy/strcmp/memset/memcpy/readline 等）；libcpp.cpp 重载 C++ 全局 new/delete 运算符，将内核态动态内存分配桥接到 PMM 的 kmalloc/kfree。

### 3.6 内核核心功能：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**该内核以 SyscallHandler 为核心抽象，通过 2048 项函数指针表分发系统调用，覆盖 exec/fork/信号/文件/网络等 Linux ABI（应用二进制接口，即程序与内核在二进制层面的调用约定）。进程管理由 ProcessManager 维护定长 PCB 池，支持 COW（写时复制，即共享内存页只在写入时复制） fork、信号私有化、文件 lease 与用户态 ELF 补丁。

### 3.7 同步与信号：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**该模块实现基于计数信号量（Semaphore）和进程等待队列（ProcessQueue）的同步原语，以及用于信号处理注册的 SigactionQueue。Semaphore 通过全局进程管理器自旋锁 pm.lock 保护，wait/signal 操作在关闭中断后持锁执行，避免与调度器竞态。

### 3.8 用户态支持：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**User 模块是运行在 RISC-V 用户态（U-mode）的应用程序及其运行时库。核心抽象包括：POS_Syscall 内联汇编封装（通过 ecall 陷入内核）、Sys_Putchar/Sys_Exit 等系统调用包装、COUT 输出流类（基于 Sys_Putchar 实现格式化输出）、以及 _UserMain 入口（调用 main 后 Sys_Exit。

### 3.9 用户态程序接口：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**user 模块是运行在内核之上的用户态测试与交互层，核心抽象是 syscall 封装库（syscall.cc + syscall_base.hh + 架构相关 syscall_arch.hh）、printf 格式化输出、以及一个庞大的测试调度框架（user_test.cc）。

### 3.10 第三方基础库：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**本仓库的 thirdparty 模块是 EASTL (Electronic Arts Standard Template Library) 的完整移植，作为内核的 C++ 标准库替代品。

### 3.11 虚拟文件系统：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**Tmp 目录是内核开发过程中的临时工作区，包含 fileobject.cpp、pmm.cpp、vfs.cpp、filepath.cpp 四个源文件，分别对应文件描述符管理、物理内存分配、VFS 节点抽象和路径解析的早期/实验版本。

### 3.12 计分板生成工具：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**scoreboard 模块是一个 Python 生成器脚本 (generate_scoreboard.py)，负责从已挂载的大赛磁盘扫描测例结构，生成 Markdown 格式的本地 scoreboard。

### 3.13 设备驱动：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**该模块是块设备驱动层，核心抽象为 DISK/VirtioDisk 封装与 SDMMC 寄存器操作。它通过 QEMU 下的 VirtIO（虚拟输入输出接口，即虚拟机中使用的标准设备协议） MMIO 环形队列和 K210 真机下的 SDIO 轮询两条路径，向上层 FAT32/ext4 提供统一的 readSector/writeSector 接口，向下经 PLIC 中断分发完成 I/O。

### 3.14 进程管理：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**本模块实现进程管理核心：Process 类（状态机、内核栈、VMS、文件描述符表、父子兄弟链表、信号队列、堆区）与 ProcessManager（定长数组 Proc[256]、分配/释放、Round-Robin 调度 Schedule）。通过 parseELF.cpp 解析 ELF 程序头并装载段到虚拟内存，支持 PT_INTERP 动态链接器加载。

### 3.15 陷阱与系统调用：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**本模块实现 RISC-V 64 的陷入（trap（陷入，即异常、中断或系统调用进入内核的统一路径））处理全链路：TrapEntry.S 汇编入口负责保存/恢复 TrapFrame 上下文，Trap.cpp 的 C 函数 Trap() 按 cause 分发中断与异常，Syscall.cpp 的 TrapFunc_Syscall 以巨型 switch 分发约 60 个系统调用。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T2026104869911125-3219` | B：人工指定历史候选 `T202410287992755-2176` |
| --- | --- | --- |
| 技术家族 | unknown | unknown |
| 系统调用数量 | — | — |
| 描述模块 | 4 | 11 |
