# T2026104869910383-1931 与人工指定历史候选 T202510003995291-2331 对比分析报告

- A（目标作品）：`T2026104869910383-1931`
- B（人工指定历史候选）：`T202510003995291-2331`
- 候选身份：年份=2025、学校=清华大学、队伍=unknown
- 候选身份来源：`metadata.md`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **49.4%**，对应“中度相似”。

模块级对照覆盖 **12** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | 启动流程 | 82% | 做法接近 |
| 2 | 信号机制 | 80% | 做法接近 |
| 3 | 系统调用层 | 80% | 做法接近 |
| 4 | 驱动框架 | 75% | 做法接近 |
| 5 | 内存管理 | 68% | 目标相同、实现路径不同 |
| 6 | 文件系统 | 65% | 目标相同、实现路径不同 |
| 7 | 网络 | 65% | 目标相同、实现路径不同 |
| 8 | 进程与任务调度 | 65% | 目标相同、实现路径不同 |
| 9 | 进程间通信 | 65% | 目标相同、实现路径不同 |
| 10 | api | 0% | 仅历史候选实现 |
| 11 | build-tools / c9d1b7c6 | 0% | 仅目标作品实现 |
| 12 | modules / lwext4 / rust | 0% | 仅历史候选实现 |

## 三、逐模块源码证据

### 3.1 启动流程：82%

**对照判断：**做法接近。 共同点：main 入口、地址空间与内核映射拷贝、ELF 加载与任务创建均一致；差异仅在测试参数解析与初始化流水线细节。

#### 相似点

- **main 入口与初始化调用链**
  - A: `src/main.rs:43-48` — 入口构造命令行参数后调用 entry::init 启动内核
  - B: `src/main.rs:27-34` — 入口固定以 busybox 执行测试命令并调用 run_user_app
- **地址空间创建与内核映射拷贝**
  - A: `kernel/src/mm/loader.rs:25-45` — new_user_aspace_empty 创建地址空间，copy_from_kernel 复制内核映射
  - B: `core/src/mm.rs:14-25` — 地址空间由平台配置创建，仅 RISC-V/x86_64 拷贝内核映射
- **ELF 加载与用户任务创建**
  - A: `kernel/src/entry.rs:33-96` — init() 创建地址空间、加载 ELF、构造 UserContext、创建 init 任务
  - B: `core/src/entry.rs:33-47` — UspaceContext 构造并关联页表根，创建任务交给调度器

#### 主要差异

- **测试运行器参数解析策略差异**
  - A: `kernel/src/entry.rs:108-150` — 按固定顺序尝试 5 个 shell 候选路径，找到即可用
  - B: `src/main.rs:27-34` — 固定以 /musl/busybox sh -c 执行编译期内嵌命令
- **初始化流水线内容差异**
  - A: `kernel/src/entry.rs:33-96` — 挂载伪文件系统、安装 glibc stub、解析 bootargs 后启动
  - B: `src/main.rs:27-34` — 先切换根文件系统、挂载、初始化 FD_TABLE，再启动用户程序

### 3.2 信号机制：80%

**对照判断：**做法接近。 两级信号管理与 rt_sig 接口一致，Stop 均桩实现；差异主要在信号帧构造方式（自研 vs 外部 crate），整体相似度高。

#### 相似点

- **进程/线程两级信号管理结构**
  - A: `vendor/starry-signal/src/api/thread.rs:25-31` — ThreadSignalManager 持有私有 pending/blocked/stack，Arc 引用进程级管理器
  - B: `core/src/process.rs:19-35` — 进程级持有 SignalActions 和跳板地址，通过 Arc 共享给所有线程
- **信号检查在陷入返回路径触发**
  - A: `kernel/src/task/signal.rs:30-40` — check_signals 在系统调用返回路径统一分发信号
  - B: `api/src/imp/task/signal.rs:25-60` — POST_TRAP 钩子每次从用户态返回时触发 check_signals
- **Stop/Continue 动作为桩实现**
  - A: `kernel/src/task/signal.rs:30-40` — Stop 直接 do_exit(1) 导致进程退出而非挂起
  - B: `api/src/imp/task/signal.rs:51-57` — Stop 直接终止进程，Continue 为空操作

#### 主要差异

- **信号帧构造与处理方式不同**
  - A: `vendor/starry-signal/src/api/thread.rs:114-122` — 在用户栈构造 SignalFrame 并 vm_write，改写 uctx 跳转 handler
  - B: `api/src/imp/task/signal.rs:25-60` — Handler 动作由外部 axsignal 内部修改 TrapFrame，本层只处理终止类动作

### 3.3 系统调用层：80%

**对照判断：**做法接近。 核心分发架构与错误码转换一致，差异在参数传递和非法号处理，故给 80 分。

#### 相似点

- **统一的 handle_syscall 分发入口**
  - A: `kernel/src/syscall/mod.rs:41-46` — 唯一入口，Sysno::new 校验后 match 分发
  - B: `src/syscall.rs:29-38` — 注册为 trap（陷入，即异常、中断或系统调用进入内核的统一路径） handler，按 Sysno 枚举 match 分发
- **统一转换为负 errno 错误码**
  - A: `kernel/src/syscall/mod.rs:41-46` — AxResult<isize> 经 unwrap_or_else 转为 -LinuxError
  - B: `src/syscall.rs:29-38` — LinuxResult<isize> 经 unwrap_or_else 转为 -err.code()
- **覆盖远超 xv6 的 Linux syscall 面**
  - A: `kernel/src/syscall/mod.rs:41-46` — 分发至 fs/mm/task/signal/ipc（进程间通信，即进程之间交换数据和同步状态的机制）/net 等子系统
  - B: `src/syscall.rs:29-38` — 分发至 undefined_os_api 下 fs/mm/net/task/signal 子模块

#### 主要差异

- **参数传递方式不同**
  - A: `kernel/src/syscall/mod.rs:41-46` — 直接透传 usize 原始参数，由实现自行解释
  - B: `src/syscall.rs:29-38` — arg0-5 转为 UserInPtr/UserOutPtr 安全指针包装
- **非法 syscall 号处理不同**
  - A: `kernel/src/syscall/mod.rs:41-46` — 非法编号直接返回 -ENOSYS
  - B: `src/syscall.rs:29-38` — 静默映射为 memfd_secret 而非报错

### 3.4 驱动框架：75%

**对照判断：**做法接近。 均以 DeviceOps trait + VFS（虚拟文件系统，即统一不同文件系统的接口层） 节点表达设备并挂 devfs，核心一致；A 扩展 poll/tty/evdev，B 用 get_device_mem 直映射，差异在接口与设备集。

#### 实现概况

- **A：**驱动框架以 pseudofs 下的 Device 节点为核心抽象：Device 包装 SimpleFsNode 与 Arc<dyn DeviceOps>，通过 read_at/write_at/ioctl/mmap（内存映射，即把文件或匿名内存映射进进程地址空间） 等方法暴露设备能力，设备即 VFS 文件节点。
- **B：**驱动框架以 VFS 伪文件系统为统一抽象层，将设备实现为 /dev 下字符设备节点，通过 DeviceOps trait 定义 read_at/write_at/get_device_mem/ioctl，并支持设备内存直映射 mmap。

### 3.5 内存管理：68%

**对照判断：**目标相同、实现路径不同。 共同点集中在 AddrSpace、缺页处理、用户内存访问门控和多模式 mmap；差异主要在映射后端组织方式与 MAP_FIXED_NOREPLACE 支持。

#### 相似点

- **AddrSpace 核心地址空间抽象**
  - A: `kernel/src/mm/aspace/mod.rs:21-26` — AddrSpace 持有 va_range/areas/pt/locked_ranges
  - B: `core/src/mm.rs:14-19` — 基于 axmm AddrSpace 创建用户地址空间
- **缺页处理按需补页**
  - A: `kernel/src/mm/aspace/mod.rs:434-468` — handle_page_fault 按后端 populate 补齐物理页
  - B: `src/mm.rs:13-30` — 缺页委托地址空间按需分配，失败发 SIGSEGV
- **用户内存访问门控**
  - A: `kernel/src/mm/access.rs:30-40` — access_user_memory 设置线程标志允许缺页
  - B: `core/src/mm.rs:209-226` — per-CPU 变量标记内核访问用户内存上下文

#### 主要差异

- **映射后端组织方式**
  - A: `kernel/src/mm/aspace/backend/mod.rs:107-114` — 通过 enum_dispatch 的 Backend 枚举分发
  - B: `api/src/imp/mm/mmap.rs:89-153` — 直接操作 axmm AddrSpace，无后端枚举
- **MAP_FIXED_NOREPLACE 支持**
  - A: `kernel/src/syscall/mm/mmap.rs:155-170` — 仅提及 FIXED 精确映射与自动搜索
  - B: `api/src/imp/mm/mmap.rs:89-153` — MAP_FIXED_NOREPLACE 不 unmap 重叠区

### 3.6 文件系统：65%

**对照判断：**目标相同、实现路径不同。 共性：FileLike 统一 fd、FlattenObjects 定长表、Slab inode；差异：挂载点树与错误码策略，故 65 分。

#### 相似点

- **FileLike 统一 fd 对象与 downcast**
  - A: `kernel/src/file/mod.rs:139-149` — 继承 Pollable+DowncastSync，统一文件/socket/pipe/epoll 等
  - B: `api/src/core/fs/fd.rs:22-29` — FileLike 提供 into_any 向下转型，fd 表容纳任意 I/O 对象
- **fd 表基于 FlattenObjects 定长数组**
  - A: `kernel/src/file/mod.rs:190-202` — 线程局部 FlattenObjects 定长数组，项含 cloexec 标志
  - B: `api/src/core/fs/fd.rs:78-78` — spin::RwLock<FlattenObjects<FdTableItem, RLIMIT_MAX_FILES>>
- **伪文件系统 Slab 分配 inode**
  - A: `kernel/src/pseudofs/fs.rs:32-57` — SimpleFs 用 Slab 分配 inode，root 由 DirMaker 延迟构建
  - B: `api/src/core/fs/imp/tmp.rs:121-129` — tmpfs 用 slab 空槽分配 inode，ino=槽位+1

#### 主要差异

- **B 显式建模挂载点树，A 轻量处理**
  - A: `kernel/src/file/fs.rs:20-28` — with_fs 仅用全局 FS_CONTEXT 切换目录，无显式 Mountpoint
  - B: `modules/vfs/src/mount.rs:66-72` — effective_mountpoint 遍历挂载链处理多次覆盖为最内层
- **from_fd 类型不匹配错误码不同**
  - A: `kernel/src/file/fs.rs:171-182` — downcast 失败区分 IsADirectory 与 BrokenPipe
  - B: `api/src/core/fs/fd.rs:43-48` — 类型不匹配统一返回 EINVAL

### 3.7 网络：65%

**对照判断：**目标相同、实现路径不同。 共享axnet协议栈、FileLike/fd表与基本socket接口，但地址族、setsockopt、Unix socket等高级能力差异大，成熟度差距明显。

#### 相似点

- **基于axnet协议栈封装Socket**
  - A: `kernel/src/file/net.rs:15-23` — Socket为axnet::Socket薄包装，Deref代理
  - B: `api/src/imp/net/socket.rs:29-32` — Socket枚举用Mutex包装Tcp/UdpSocket
- **通过FileLike接入通用fd表**
  - A: `kernel/src/file/net.rs:15-23` — 实现FileLike/Pollable，统一fd与poll
  - B: `api/src/imp/net/socket.rs:187-193` — 实现FileLike，read/write映射recv/send
- **sys_socket创建入口**
  - A: `kernel/src/syscall/net/socket.rs:26` — sys_socket按domain/type分派
  - B: `api/src/imp/net/socket.rs:278` — sys_socket分发TCP/UDP

#### 主要差异

- **地址族支持范围不同**
  - A: `kernel/src/syscall/net/addr.rs:34-40` — read_family分派AF_INET/INET6/UNIX/VSOCK
  - B: `api/src/imp/net/socket.rs:231-235` — into_sockaddr对IPv6直接panic
- **Unix socket与SCM_RIGHTS缺失**
  - A: `kernel/src/syscall/net/socket.rs:173` — sys_socketpair创建Unix socket对
  - B: `api/src/imp/net/socket.rs:29-32` — Socket仅含Tcp/Udp变体
- **setsockopt实现真实性**
  - A: `kernel/src/syscall/net/opt.rs:161-167` — getsockopt检查长度并设置optlen
  - B: `api/src/imp/net/socket.rs:160-167` — setsockopt所有level返回Ok

### 3.8 进程与任务调度：65%

**对照判断：**目标相同、实现路径不同。 两仓均基于 axtask 实现 POSIX（可移植操作系统接口，即 Unix 类系统通用接口规范） 进程模型，共享 TaskExt 挂接和全局表思路；但结构层次、引用策略与特性集差异明显，故定 65 分。

#### 相似点

- **通过 TaskExt 挂接 axtask 调度器**
  - A: `kernel/src/task/mod.rs:318-330` — 将 Thread 扩展为 TaskExt，切换资源作用域
  - B: `core/src/task.rs:19-30` — TaskExt 绑定内核任务与 POSIX 线程
- **维护 PID（进程标识符，即系统用于区分进程的编号）/PGID/SID 全局查找表**
  - A: `kernel/src/task/ops.rs:22-32` — 四张 WeakMap 全局表，含任务/进程/组/会话
  - B: `process/src/thread.rs:40-47` — 线程表用 Mutex<BTreeMap> 保护，全局可见
- **实现进程退出与回收**
  - A: `kernel/src/task/ops.rs:259-272` — do_exit 处理 clear_child_tid、robust list、通知父进程
  - B: `process/src/process.rs:160-160` — exit 移交子进程给 reaper 并标记 zombie

#### 主要差异

- **进程模型层次不同**
  - A: `kernel/src/task/mod.rs:150-163` — Thread 为唯一执行单位，ProcessData 承载共享资源
  - B: `process/src/lib.rs:1-5` — 规定 Session→ProcessGroup→Process→Thread 四级包含层级
- **全局表引用策略不同**
  - A: `kernel/src/task/ops.rs:22-32` — 使用 WeakMap 弱引用，避免残留强引用
  - B: `process/src/thread.rs:40-47` — 使用 Mutex+BTreeMap 持有 Arc 强引用
- **线程私有状态丰富度不同**
  - A: `kernel/src/task/mod.rs:150-163` — Thread 含 robust list、capability、TimeManager 等状态
  - B: `process/src/thread.rs:7-7` — Thread 仅含 tid 和指向进程的 Weak 引用

### 3.9 进程间通信：65%

**对照判断：**目标相同、实现路径不同。 共享内存与 futex（快速用户态互斥量，即用户态原子操作配合内核等待唤醒的同步机制） 核心机制对应，但映射方式、futex 表组织及权限检查差异显著。

#### 相似点

- **futex 等待/唤醒机制**
  - A: `kernel/src/task/futex.rs:222-236` — FutexKey 区分私有/共享，WaitQueue 实现挂起与唤醒。
  - B: `api/src/imp/task/futex.rs:33-47` — FUTEX_WAIT 校验地址后挂起，WAKE 唤醒后 yield。
- **共享内存全局管理器**
  - A: `kernel/src/syscall/ipc/shm.rs:230` — ShmManager 用 BiBTreeMap 维护 key↔shmid 等映射。
  - B: `core/src/shared_memory.rs:20` — SharedMemoryManager 用 BTreeMap 按 key 索引共享段。
- **IPC 标识符原子分配**
  - A: `kernel/src/syscall/ipc/mod.rs:16-18` — next_ipc_id 原子计数器分配唯一 ID。
  - B: `core/src/shared_memory.rs:30-39` — next_available_key 用 fetch_add 原子分配 key。

#### 主要差异

- **IPC 权限检查实现**
  - A: `kernel/src/syscall/ipc/mod.rs:66-70` — 按 uid/gid 匹配 owner/group/other 位，root 放行。
  - B: `api/src/interface/mm/shm.rs:26-33` — sys_shmget 权限检查 TODO，未实现。
- **futex 表组织方式**
  - A: `kernel/src/task/futex.rs:222-236` — 私有/共享 futex 分别挂进程表或全局 SHARED_FUTEX_TABLES。
  - B: `api/src/imp/task/futex.rs:18-26` — 每进程单一 futex_table，以用户地址为键。
- **共享内存映射实现路径**
  - A: `kernel/src/syscall/ipc/shm.rs:475-483` — SharedPages 物理页集合复用，Backend::new_shared 映射。
  - B: `api/src/interface/mm/shm.rs:108-113` — 内核虚拟地址经 virt_to_phys 转物理地址后映射。

### 3.10 api：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**api 模块是 Starry 内核的系统调用接口层，核心抽象是 UserPtr/UserConstPtr 指针封装（通过 PtrWrapper trait 对用户态地址做合法性校验后转换为内核可访问指针）以及 interface/imp 两层分离的 syscall 分发结构。

### 3.11 build-tools / c9d1b7c6：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**`_build-tools` 是一个以空壳包 `build-tools`（全部源码仅 `fn main() {}`）为载体的 Cargo 工作区，核心价值是锁定并离线自举两个 ArceOS 构建期工具：cargo-axplat（解析平台包配置路径）与 axconfig-gen（读取/生成平台 TOML 配置）。

### 3.12 modules / lwext4 / rust：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**该模块将 C 语言编写的 lwext4 (ext2/3/4 文件系统库) 通过 FFI 封装为 Rust 接口，核心抽象是 Ext4BlockDevice (块设备适配层)、Ext4Filesystem (文件系统操作入口) 和 InodeRef (inode 引用封装)。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T2026104869910383-1931` | B：人工指定历史候选 `T202510003995291-2331` |
| --- | --- | --- |
| 技术家族 | arceos-starry | arceos-starry |
| 系统调用数量 | 212 | 212 |
| 描述模块 | 10 | 11 |
