# T2026104879911127-1444 与人工指定历史候选 T202510216995249-4014 对比分析报告

- A（目标作品）：`T2026104879911127-1444`
- B（人工指定历史候选）：`T202510216995249-4014`
- 候选身份：年份=2025、学校=燕山大学、队伍=unknown
- 候选身份来源：`metadata.md`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **38.7%**，对应“低度相似”。

模块级对照覆盖 **9** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | 信号机制 | 90% | 做法接近 |
| 2 | 系统调用层 | 88% | 做法接近 |
| 3 | 内存管理 | 85% | 做法接近 |
| 4 | 启动流程 | 78% | 做法接近 |
| 5 | 进程与任务调度 | 78% | 做法接近 |
| 6 | 进程间通信 | 70% | 目标相同、实现路径不同 |
| 7 | 网络 | 60% | 目标相同、实现路径不同 |
| 8 | 驱动框架 | 50% | 目标相同、实现路径不同 |
| 9 | 文件系统 | 45% | 目标相同、实现路径不同 |

## 三、逐模块源码证据

### 3.1 信号机制：90%

**对照判断：**做法接近。 双层管理器、12个信号系统调用、check_signals 派发等核心机制一致；仅外部 crate 来源与 sigreturn 恢复细节不同。

#### 相似点

- **进程/线程双层信号管理器**
  - A: `core/src/task.rs:239-242` — ProcessSignalManager 在 ProcessData::new 中创建并传入 SIGNAL_TRAMPOLINE
  - B: `core/src/task.rs:155-158` — ThreadData 与 ProcessData 分别持有两级管理器，同样传入 restorer
- **12 个信号系统调用封装**
  - A: `api/src/syscall/signal.rs:70` — rt_sigaction 等 12 个 syscall 集中在 signal.rs
  - B: `api/src/imp/signal.rs:63` — rt_sigaction 等 12 个 syscall 实现在 imp/signal.rs
- **check_signals 统一派发**
  - A: `api/src/signal.rs:10` — 陷入返回后调用 check_signals 检查并派发信号
  - B: `api/src/signal.rs:13` — POST_TRAP 回调中执行信号处理

#### 主要差异

- **sigreturn 恢复方式不同**
  - A: `api/src/signal.rs:43-51` — 用全局 AtomicBool 阻止下一轮 check_signals，避免递归
  - B: `vendor/axsignal/src/api/thread.rs:10` — SignalFrame 压入用户栈，sigreturn 从中恢复上下文

### 3.2 系统调用层：88%

**对照判断：**做法接近。 共同点：统一 syscall 分发、用户指针封装、负错误码转换；差异在陷入上下文来源和指针校验粒度。

#### 相似点

- **统一系统调用分发入口**
  - A: `api/src/syscall/mod.rs:22-35` — handle_syscall 匹配 Sysno 分发并统一转负错误码
  - B: `src/syscall.rs:10-15` — 通过宏注册陷入入口，match Sysno 分发
- **用户态指针安全封装**
  - A: `api/src/syscall/io_mpx/select.rs:15-21` — 用 UserPtr/UserConstPtr 封装且 nullable! 处理空指针
  - B: `api/src/ptr.rs:9-21` — UserPtr/UserConstPtr 解引用前做页表级权限校验
- **错误统一转换为 Linux 负值**
  - A: `api/src/syscall/mod.rs:22-35` — 末尾将 AxResult 错误转换为 Linux 负错误码
  - B: `src/syscall.rs:10-15` — 末尾统一将 LinuxResult 转为负错误码

#### 主要差异

- **陷入上下文来源不同**
  - A: `api/src/syscall/mod.rs:22-35` — 使用 axhal::uspace 的 UserContext 保存寄存器现场
  - B: `arceos/modules/axhal/src/arch/riscv/context.rs:80-82` — 使用 TrapFrame 的 arg0-arg5 方法从 a0-a5 读参数
- **用户指针校验粒度不同**
  - A: `api/src/syscall/io_mpx/select.rs:15-21` — nullable! 宏处理空指针，依赖封装类型避免裸指针
  - B: `api/src/ptr.rs:9-21` — check_region 做页表权限校验并 populate_area 补齐页面

### 3.3 内存管理：85%

**对照判断：**做法接近。 核心机制一致：axmm 地址空间、brk（堆边界调整，即通过移动边界扩展或收缩进程堆） 仅更新堆顶、缺页处理标志；仅 mmap（内存映射，即把文件或匿名内存映射进进程地址空间） 标志校验策略有差异。

#### 相似点

- **地址空间核心抽象一致**
  - A: `core/src/task.rs:177-183` — ProcessData 以 Arc<Mutex<AddrSpace>> 持有地址空间
  - B: `core/src/task.rs:191-195` — 同样以 Arc<Mutex<AddrSpace>> 存储，多线程共享
- **brk 仅更新堆顶指针**
  - A: `api/src/syscall/mm/brk.rs:5-16` — 只更新 heap_top，不实际映射/释放页
  - B: `api/src/imp/mm/brk.rs:4-14` — 同样只调整 heap_top，堆已预映射
- **缺页处理区分内核态访问**
  - A: `api/src/mm.rs:240-256` — 通过 is_accessing_user_memory 区分上下文
  - B: `core/src/mm.rs:222-233` — ACCESSING_USER_MEM 每 CPU 变量标记

#### 主要差异

- **mmap 未知标志位校验策略不同**
  - A: `api/src/syscall/mm/mmap.rs:106-115` — SHARED_VALIDATE 严格校验，普通 SHARED 截断容忍
  - B: `api/src/imp/mm/mmap.rs:70-86` — from_bits_truncate 静默丢弃未知标志位

### 3.4 启动流程：78%

**对照判断：**做法接近。 核心启动流程（建地址空间、加载 ELF、spawn 任务）一致；差异在启动分流方式与进程线程创建模型。

#### 相似点

- **编译期参数注入启动控制流**
  - A: `src/main.rs:22-34` — AX_CMDLINE 编译期注入，main() 静态分流评测/调试路径
  - B: `src/main.rs:15-23` — AX_TESTCASES_LIST 编译期环境变量驱动测试用例列表
- **用户程序初始化核心流程一致**
  - A: `src/entry.rs:17-31` — run_initproc 建地址空间、加载 ELF、构造 UserContext 并 spawn
  - B: `src/entry.rs:13-20` — run_user_app 建地址空间、加载 ELF、构造 UspaceContext 并 spawn
- **共用 load_user_app 加载接口**
  - A: `core/src/mm.rs:230-330` — load_user_app 处理 shebang/动态链接，映射栈堆
  - B: `core/src/mm.rs:100-100` — load_user_app 读取 ELF、处理 shebang/动态链接器，返回入口

#### 主要差异

- **启动路径组织方式不同**
  - A: `src/main.rs:22-34` — main() 按 AX_CMDLINE 分流评测/调试，不循环迭代
  - B: `src/main.rs:15-23` — main() 创建 init 进程后遍历 AX_TESTCASES_LIST 逐个运行
- **任务创建模型不同**
  - A: `src/entry.rs:17-31` — run_initproc 直接构造并 spawn 首个用户任务
  - B: `src/entry.rs:13-20` — run_user_app 通过 fork 建进程、new_thread 建线程后 spawn

### 3.5 进程与任务调度：78%

**对照判断：**做法接近。 核心机制一致（WeakMap、clone/exit 语义），但绑定与地址空间共享实现不同，相似度高。

#### 相似点

- **sys_clone 低 8 位提取 exit_signal**
  - A: `api/src/syscall/task/clone.rs:88-98` — 低 8 位作退出信号，按 CLONE_THREAD 等分支。
  - B: `api/src/imp/task/clone.rs:90-99` — 同样低 8 位作 exit_signal，强制 CLONE_THREAD 组合。
- **do_exit 清理 clear_child_tid 并通知父进程**
  - A: `api/src/task.rs:161-173` — 先唤醒 futex（快速用户态互斥量，即用户态原子操作配合内核等待唤醒的同步机制） 再遍历 robust list，判断通知父进程。
  - B: `api/src/imp/task/exit.rs:37-44` — 清除 tid 后发 exit_signal 并唤醒 child_exit_wq。

#### 主要差异

- **地址空间共享策略差异**
  - A: `api/src/syscall/task/clone.rs:88-98` — CLONE_THREAD 复用父进程 ProcessData 和页表根。
  - B: `api/src/imp/task/clone.rs:152-159` — 非 CLONE_VM 时 try_clone 后复制内核映射，无完整 COW（写时复制，即共享内存页只在写入时复制）。

### 3.6 进程间通信：70%

**对照判断：**目标相同、实现路径不同。 同为实现 System V（一组经典 Unix 系统接口约定） shm+futex+pipe，但物理页管理与共享 futex 等实现路径不同，故 70 分。

#### 相似点

- **管道基于环形缓冲**
  - A: `api/src/file/pipe.rs:28-28` — Pipe 通过 Arc<Shared> 共享 HeapRb 环形缓冲
  - B: `api/src/file/pipe.rs:77-80` — Arc<Mutex<PipeRingBuffer>> 共享 256 字节环形缓冲

#### 主要差异

- **共享内存物理页管理**
  - A: `api/src/syscall/ipc/shm.rs:144-156` — 首个 attach 创建 SharedPages，后续克隆复用物理页
  - B: `core/src/shm.rs:100-103` — 直接 alloc_pages 分配物理页并转换为物理地址
- **futex 共享/私有区分**
  - A: `core/src/futex.rs:109-146` — FutexKey 区分私有/共享，共享用物理页弱引用标识
  - B: `core/src/futex.rs:8-8` — FutexTable 为进程级存储，未区分共享地址
- **shm 权限检查处理**
  - A: `api/src/syscall/ipc/shm.rs:43-58` — 未处理 IPC_CREAT/EXCL，权限位解析不完整
  - B: `api/src/imp/mm/shm.rs:32-35` — 硬编码 UID=0/GID=0 调用权限检查，实际绕过

### 3.7 网络：60%

**对照判断：**目标相同、实现路径不同。 共用 axnet 封装与 SocketAddrExt 地址转换，但 A 有完整 syscall 层与更多类型，B 缺失 syscall，路径不同。

#### 相似点

- **Socket 包装 axnet 套接字实现 FileLike**
  - A: `api/src/file/net.rs:25-36` — Socket 实现 FileLike，read/write 映射到 recv/send，并实现 Pollable。
  - B: `api/src/file/net.rs:12-26` — Socket 枚举用 Mutex 包装 Tcp/Udp，宏生成方法实现 FileLike。
- **SocketAddrExt trait 统一 sockaddr 读写**
  - A: `api/src/socket.rs:20-29` — trait 统一 IPv4/IPv6/Unix/vsock 地址族的用户态安全读写。
  - B: `api/src/socket.rs:38-52` — 先拷贝到内核栈缓冲，再按地址族解析，避免直接解引用用户指针。

#### 主要差异

- **syscall 层网络系统调用有无**
  - A: `api/src/syscall/net/socket.rs:26-37` — sys_socket 按 domain/type 分派，处理 NONBLOCK/CLOEXEC 标志。
  - B: `src/syscall.rs:11-19` — 分发表无 socket/bind/connect 等网络调用，用户态无法创建 socket。
- **套接字类型覆盖范围不同**
  - A: `api/src/syscall/net/socket.rs:26-37` — 分派到 TCP/UDP/Unix/vsock 四种类型。
  - B: `api/src/file/net.rs:12-26` — Socket 枚举仅含 Udp/Tcp 两个变体，无 Unix/vsock。

### 3.8 驱动框架：50%

**对照判断：**目标相同、实现路径不同。 共同基于 axdriver 设备模型与初始化流程，但 A 侧重 devfs 设备抽象与 evdev，B 侧重设备聚合与分发，路径明显不同。

#### 相似点

- **共同基于 axdriver 设备模型**
  - A: `arceos/modules/axdriver/src/structs/dyn.rs:16` — 使用 Box<dyn Trait> 动态分发模型，支持异构设备集合
  - B: `arceos/modules/axdriver/src/lib.rs:1-184` — AllDevices 支持静态与动态两种设备模型
- **均通过容器收集设备实例**
  - A: `arceos/modules/axinput/src/lib.rs:17-19` — 全局静态容器收集所有输入设备，take_inputs 取出
  - B: `arceos/modules/axdriver/src/lib.rs:1-184` — AllDevices 内含 AxDeviceContainer 按类别持有设备

#### 主要差异

- **A 构建 DeviceOps 抽象，B 无此层**
  - A: `core/src/vfs/dev.rs:28-34` — 定义 DeviceOps trait 提供读写/ioctl/mmap/poll
  - B: `arceos/modules/axdriver/src/lib.rs:1-184` — AllDevices 仅按类别组织设备，无文件操作抽象
- **设备类别侧重不同**
  - A: `api/src/vfs/dev/event.rs:24-28` — 实现 evdev 输入事件设备，含 read_ahead 缓存
  - B: `arceos/modules/axdriver/src/lib.rs:1-184` — AllDevices 仅含 net/block/display 三类，未涉及 input
- **设备节点注册机制不同**
  - A: `api/src/vfs/dev/mod.rs:291-296` — devfs builder 运行时动态生成 /dev/input 目录
  - B: `arceos/modules/axdriver/src/lib.rs:1-184` — 通过 Cargo feature 编译期决定哪些设备类别启用

### 3.9 文件系统：45%

**对照判断：**目标相同、实现路径不同。 共享 FileLike/FD_TABLE 抽象与目录遍历接口，但底层存储（内存 FS vs axfs/FAT）与 mount 语义差异显著。

#### 相似点

- **全局文件描述符表 FD_TABLE**
  - A: `api/src/file/mod.rs:294-300` — get_file_like 通过 fd 索引获取 Arc<dyn FileLike>，返回 BadFileDescriptor。
  - B: `api/src/file/mod.rs:149-151` — FD_TABLE 用 FlattenObjects 管理稀疏 fd，上限 AX_FILE_LIMIT=1024。
- **目录项遍历接口**
  - A: `core/src/vfs/dir.rs:142-146` — read_dir 先输出 . 和 ..，支持 offset 跳过已读项，实现 getdents64 增量遍历。
  - B: `api/src/imp/fs/ctl.rs:169-176` — getdents64 写入 linux_dirent64，d_ino 和 d_off 固定（FIXME）。

#### 主要差异

- **底层存储实现**
  - A: `core/src/vfs/fs.rs:32-37` — SimpleFs 用 slab 分配 inode 号，纯内存文件系统，无磁盘设备层。
  - B: `api/src/imp/fs/mount.rs:110-117` — mount_fat_fs 挂载 FAT 文件系统，但仅记录挂载点，不真正切换视图。
- **mount 语义**
  - A: `api/src/vfs/mod.rs:18-25` — 挂载前若目标路径不存在先创建目录，真正挂载虚拟文件系统。
  - B: `api/src/imp/fs/mount.rs:110-117` — 仅将挂载记录推入 MOUNTED 向量，不切换文件系统视图，check_mounted 用前缀匹配。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T2026104879911127-1444` | B：人工指定历史候选 `T202510216995249-4014` |
| --- | --- | --- |
| 技术家族 | arceos-starry | arceos-starry |
| 系统调用数量 | 190 | 98 |
| 描述模块 | 9 | 9 |
