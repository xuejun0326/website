# T202610112999587-1186 与人工指定历史候选 T202510247995532-2817 对比分析报告

- A（目标作品）：`T202610112999587-1186`
- B（人工指定历史候选）：`T202510247995532-2817`
- 候选身份：年份=unknown、学校=unknown、队伍=unknown
- 候选身份来源：`metadata_unavailable`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **36.4%**，对应“低度相似”。

模块级对照覆盖 **13** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | 系统调用层 | 60% | 目标相同、实现路径不同 |
| 2 | 文件系统 | 55% | 目标相同、实现路径不同 |
| 3 | 内存管理 | 50% | 目标相同、实现路径不同 |
| 4 | 启动流程 | 50% | 目标相同、实现路径不同 |
| 5 | 信号机制 | 45% | 目标相同、实现路径不同 |
| 6 | 进程与任务调度 | 40% | 目标相同、实现路径不同 |
| 7 | 进程间通信 | 40% | 目标相同、实现路径不同 |
| 8 | 驱动框架 | 15% | 独立设计 |
| 9 | 网络 | 7% | 独立设计 |
| 10 | general | 0% | 仅目标作品实现 |
| 11 | hal | 0% | 仅目标作品实现 |
| 12 | libs | 0% | 仅目标作品实现 |
| 13 | src / kernel | 0% | 仅历史候选实现 |

## 三、逐模块源码证据

### 3.1 系统调用层：60%

**对照判断：**目标相同、实现路径不同。 共同点：表驱动分发与trap（陷入，即异常、中断或系统调用进入内核的统一路径）参数提取；差异：链接节vs静态表注册、异步vs同步handler，故60分。

#### 相似点

- **表驱动syscall分发**
  - A: `general/src/syscall.rs:316-320` — SYSCALL_TABLE为512项AtomicUsize函数指针表，启动期注册
  - B: `src/kernel/syscall.rs:15-37` — 链接节收集后构建[Option<SyscallHandler>;512]查表
- **trap层提取syscall号和参数**
  - A: `arch/src/riscv64/trap/exception.rs:437-444` — 从trap frame读a7和satp，dispatch后处理返回
  - B: `crates/eonix_hal/src/arch/riscv64/trap/trap_context.rs:106-119` — 从a7读syscall号，a0-a5读参数，符合RISC-V ABI（应用二进制接口，即程序与内核在二进制层面的调用约定）
- **多架构trap入口支持**
  - A: `arch/src/loongarch64/trap/exception.rs:330-360` — LoongArch入口dispatch前恢复中断，返回前关中断处理IPI
  - B: `crates/eonix_hal/src/arch/loongarch64/trap/trap_context.rs:102-115` — 同样从a7/a0-a5读取，trap_type统一分类为Syscall

#### 主要差异

- **注册机制：静态表vs链接器节**
  - A: `general/src/syscall.rs:316-320` — register_syscall用compare_exchange写AtomicUsize数组，重复注册panic
  - B: `macros/src/lib.rs:65-70` — define_syscall宏将handler放入独立链接节，依赖链接器收集
- **handler模型：同步vs异步future**
  - A: `general/src/syscall.rs:501-507` — 热路径将AtomicUsize transmute为同步函数指针直接调用
  - B: `src/kernel/syscall.rs:15-37` — handler返回Pin<Box<dyn Future+Send>>，用ThreadAlloc分配future

### 3.2 文件系统：55%

**对照判断：**目标相同、实现路径不同。 共同 VFS（虚拟文件系统，即统一不同文件系统的接口层） 抽象与 ext/FAT 实现，但 ext 实现依赖外部 crate、fat32 只读、缓存机制不同，故判为 divergent。

#### 相似点

- **VFS 抽象层与 inode 接口**
  - A: `libs/extfs/src/state.rs:1-4` — extfs 通过 FsDriver trait 接入 vfs 抽象层
  - B: `src/fs/mod.rs:1-5` — 模块通过 Vfs/Inode trait 定义统一接口
- **都实现 FAT 文件系统**
  - A: `libs/fatfs/src/inode.rs:1-3` — fatfs 文件 inode 记录父目录槽位以回写
  - B: `src/fs/fat32.rs:1-8` — 自研 fat32 解析引导扇区与 FAT 表
- **都有缓存层降低磁盘访问**
  - A: `libs/extfs/src/state.rs:84-89` — BlockCache 用 pending_writebacks 保证一致性
  - B: `src/fs/ext4.rs:344-355` — PageCache 通过 read_direct 绕缓存读盘

#### 主要差异

- **ext 实现路径不同**
  - A: `libs/extfs/src/inode.rs:159-162` — 自研 ext4 inode 校验和算法与 Linux 一致
  - B: `src/fs/ext4.rs:46-50` — ext4 通过 another_ext4 crate 适配，非自研
- **FAT 写支持不同**
  - A: `libs/fatfs/src/inode.rs:248-272` — mkdir 直接写 . 和 .. 条目，支持写目录操作
  - B: `src/fs/fat32.rs:310-316` — fat32 只读，is_read_only 为 true，写入未实现
- **缓存一致性机制不同**
  - A: `libs/extfs/src/state.rs:84-89` — 用 pending_writebacks 与 active_direct_writes 跟踪写回
  - B: `src/fs/ext4.rs:304-313` — 采用 10 秒惰性同步，write 路径立即 sync

### 3.3 内存管理：50%

**对照判断：**目标相同、实现路径不同。 共同点：buddy物理页分配与slab小对象分配；差异：A重记账/锁顺序，B重用户COW（写时复制，即共享内存页只在写入时复制）/mmap（内存映射，即把文件或匿名内存映射进进程地址空间）/per-cpu。

#### 相似点

- **物理页分配采用伙伴系统**
  - A: `libs/allocator/src/lib.rs:288-300` — phys 子分配器为 BuddyAllocator，管理物理页帧
  - B: `src/kernel/mem/page_alloc.rs:58-73` — PerCpuPageAlloc 批量从全局 BUDDY_ALLOC 补充
- **内核小对象通过 slab 分配**
  - A: `libs/allocator/src/slab.rs:166-173` — slab 页目录三层稀疏结构，小对象分配
  - B: `src/kernel/mem/allocator.rs:16-33` — ≤2048 字节走 slab，大块直接分配页

#### 主要差异

- **分配记账机制不同**
  - A: `libs/allocator/src/registry.rs:18-35` — registry 64 分片维护指针到分配记录
  - B: `src/kernel/mem/page_alloc/raw_page.rs:65-74` — RawPage 仅含 flags/refcount，无分配来源记录
- **虚拟地址空间范围不同**
  - A: `libs/allocator/src/lib.rs:564-581` — vmem 仅初始化内核虚拟地址空间
  - B: `src/kernel/mem/mm_area.rs:93-115` — handle_cow 处理用户 COW 页错误
- **并发控制策略不同**
  - A: `libs/allocator/src/lib.rs:115-139` — RelaxStrategy 自旋等待时处理紧急工作
  - B: `src/kernel/mem/page_alloc.rs:58-73` — 分配时关抢占保护 percpu 数据

### 3.4 启动流程：50%

**对照判断：**目标相同、实现路径不同。 共享交接抽象与 Sv48 双映射页表，但交接对象复杂度、固件处理和入口机制差异明显。

#### 相似点

- **平台无关交接对象**
  - A: `general/src/start.rs:1-5` — StartContext 作为架构与平台无关层的交接契约，一次性填充后按顺序消费
  - B: `crates/eonix_hal/src/bootstrap.rs:5-17` — BootStrapData 传递早期栈和页分配器给内核主逻辑，含访问器
- **RISC-V 早期 Sv48 页表建立**
  - A: `arch/src/riscv64/boot.rs:6-16` — 裸汇编手工搭建 Sv48 最小页表，identity 与高半区共享 PMD
  - B: `crates/eonix_hal/src/arch/riscv64/bootstrap.rs:78-95` — 入口裸汇编加载静态页表到 satp 开启 Sv48 分页

#### 主要差异

- **交接对象内容复杂度**
  - A: `general/src/start.rs:270-270` — StartContext 含 boot/firmware/memory/address/allocator 五部分
  - B: `crates/eonix_hal/src/bootstrap.rs:6-6` — BootStrapData 仅含 early_stack 和 allocator，结构简单
- **固件处理方式**
  - A: `kernel/src/dtb/mod.rs:44-51` — DTB 路径从 StartContext 提取固件视图，不匹配则 panic
  - B: `crates/eonix_hal/src/arch/x86_64/bootstrap.rs:491-511` — 无固件视图抽象，直接用引导加载器传入的 E820 内存映射
- **交接入口机制**
  - A: `kernel/src/main.rs:230-237` — __kernel_start_init 空指针检查后按固件来源分派
  - B: `crates/eonix_hal/eonix_hal_macros/src/lib.rs:98-105` — 属性宏将主函数重命名为 _eonix_hal_main 供架构启动代码调用

### 3.5 信号机制：45%

**对照判断：**目标相同、实现路径不同。 同为基础信号机制，但状态组织、投递路径与 syscall 支持差异显著，仅共享 POSIX（可移植操作系统接口，即 Unix 类系统通用接口规范） 语义框架。

#### 相似点

- **均维护 pending 队列与屏蔽字**
  - A: `libs/sched/src/signal.rs:295-304` — SignalState 含 pending 位图+队列与 blocked 掩码
  - B: `src/kernel/task/signal.rs:71-94` — SignalListInner 含 pending 队列与 mask，raise 入队
- **均提供 sigaction 动作表**
  - A: `libs/sched/src/signal.rs:511-517` — SharedSignal 持有 sigaction 表，支持 CLONE_SIGHAND
  - B: `src/kernel/task/signal/signal_action.rs:68-76` — SignalAction 三态枚举，SimpleHandler 含地址和 mask

#### 主要差异

- **信号集合表示：位图 vs 堆队列**
  - A: `libs/sched/src/signal.rs:295-304` — 位图提供 O(1) 判断，队列保存 SigInfo
  - B: `src/kernel/task/signal.rs:71-94` — BinaryHeap<Reverse<Signal>> 优先级队列
- **sigaction 表共享语义不同**
  - A: `libs/sched/src/signal.rs:511-517` — Arc 共享支持 CLONE_SIGHAND
  - B: `src/kernel/task/signal.rs:311-328` — fork 时深拷贝 actions，无共享
- **SA_SIGINFO 支持差异**
  - A: `libs/sched/src/signal.rs:295-304` — SigInfo 携带 sender_pid/uid/raw payload
  - B: `src/kernel/task/signal/signal_action.rs:250-252` — set_siginfo 返回 EINVAL，不支持

### 3.6 进程与任务调度：40%

**对照判断：**目标相同、实现路径不同。 共同点限于退出与进程组管理；调度模型、任务身份、并发控制差异显著，故给 40 分。

#### 相似点

- **进程/线程退出处理**
  - A: `libs/sched/src/operation.rs:269-275` — exit 控制流不返回，drop 自身 Arc 后 schedule_once
  - B: `src/kernel/task/process_list.rs:120-190` — 最后一个线程退出时执行孤儿收养和资源释放
- **进程组/会话管理**
  - A: `libs/sched/src/operation.rs:214-235` — 进程组对象未单独登记，从存活任务线性扫描反查
  - B: `src/kernel/task/process.rs:412-415` — setsid 替换 session/pgroup 后 call_rcu 延迟释放旧值

#### 主要差异

- **调度模型：抢占式 vs 协作式**
  - A: `libs/sched/src/scheduler.rs:3085-3090` — schedule_once 从 runqueue 选下一个任务切换，关闭本地中断
  - B: `src/kernel/task/thread.rs:357-374` — real_run 循环中定时器中断时主动 yield_now 让出 CPU
- **任务身份模型**
  - A: `libs/sched/src/task.rs:776-778` — Task 即调度实体，身份即 Arc<Task>，无全局 PID（进程标识符，即系统用于区分进程的编号） 表
  - B: `src/kernel/task/process.rs:43-53` — Process 持有 pid 并分 Process/Thread 两层
- **并发控制策略**
  - A: `libs/sched/src/task.rs:502-512` — 任务状态机由原子 CAS 驱动，状态转换不依赖 rq 锁
  - B: `src/kernel/task/process.rs:507-515` — session/pgroup/parent 方法要求 Proof 参数，类型层面强制持锁

### 3.7 进程间通信：40%

**对照判断：**目标相同、实现路径不同。 共享内存与管道存在对应接口，但底层存储（稀疏块vs tmpfs）及同步机制（信号量 vs futex（快速用户态互斥量，即用户态原子操作配合内核等待唤醒的同步机制））差异大，shmctl完整度不同。

#### 相似点

- **共享内存映射接口**
  - A: `kernel/src/syscalls/ipc.rs:75-85` — sys_shmat将ShmObject作为FileLike映射
  - B: `src/kernel/syscall/mm.rs:216-237` — shmat通过mmap_fixed映射共享内存文件
- **管道文件抽象**
  - A: `libs/vfs/src/pipe.rs:40-40` — Pipe结构实现读写等待队列
  - B: `src/kernel/vfs/filearray.rs:285-299` — FileArray::pipe分配fd并创建Pipe
- **全局共享内存管理器**
  - A: `general/src/ipc/shm.rs:280-280` — ShmManager维护by_id/by_key表
  - B: `src/fs/shm.rs:38-38` — ShmManager持有tmpfs和BTreeMap

#### 主要差异

- **共享内存存储模型**
  - A: `general/src/ipc/shm.rs:166-173` — 稀疏块BTreeMap存储，未写页补零
  - B: `src/fs/shm.rs:37-66` — 直接以tmpfs FileInode为载体
- **shmctl实现完整度**
  - A: `kernel/src/syscalls/ipc.rs:125-130` — 处理IPC_STAT/IPC_SET/IPC_RMID
  - B: `src/kernel/syscall/mm.rs:281-283` — shmctl直接返回Ok(0)空壳
- **attach计数维护方式**
  - A: `general/src/ipc/shm.rs:440-442` — on_mapped/on_unmapped维护nattch
  - B: `src/fs/shm.rs:57-81` — ShmIdDs字段从未更新

### 3.8 驱动框架：15%

**对照判断：**独立设计。 两者仅表层共享驱动 trait 与块设备对象，核心抽象（块队列 vs PCI（外设组件互连，即枚举和访问设备的总线标准） 总线）和 I/O 模型完全不同，相似度低。

#### 相似点

- **定义驱动接口 trait**
  - A: `general/src/dev/block.rs:380` — BlockDriver trait 定义纯异步队列接口
  - B: `src/kernel/pcie/driver.rs:14-34` — PCIDriver trait 定义 vendor/device ID 匹配与初始化
- **提供块设备抽象**
  - A: `general/src/dev/block.rs:610` — 聚合几何、限制、属性等设备能力
  - B: `src/kernel/block.rs:38` — 支持磁盘和分区，通过 devid 唯一标识

#### 主要差异

- **核心抽象目标不同**
  - A: `general/src/dev/block.rs:390` — queue_bio 将 Bio 请求提交到硬件队列
  - B: `src/kernel/pcie/driver.rs:14-34` — 以 (vendor<<16|device) 为键注册并回放设备
- **I/O 完成模型不同**
  - A: `general/src/dev/block.rs:700` — submit_bio_wait 同步等待完成，带 bounded poll
  - B: `src/driver/ahci/register.rs:45` — spinwait_clear 忙等轮询寄存器位清零

### 3.9 网络：7%

**对照判断：**独立设计。 双方模块职责存在对应关系，但描述中的接口和实现组织重合较少。

#### 相似点

- **实现位置与职责对照**
  - A: `libs/net/src/socket/mod.rs:465-465` — 批量将指定代际的 socket 置为断网状态，返回受影响数量。
  - B: `src/net/iface.rs:100-100` — 为指定端口和类型创建 socket，检查端口冲突，bind_port=0 时自动分配。
- **实现位置与职责对照**
  - A: `libs/net/src/transport/udp.rs:508-523` — UDP 多播/广播接收者上限为 16，超出部分被静默截断，大型多播组可能丢失数据且无错误反馈。
  - B: `src/net/socket/udp.rs:17-17` — 全局端口到 BoundSocket 映射表，实现多 socket 共享同一 UDP 端口。
- **实现位置与职责对照**
  - A: `libs/net/src/transport/udp.rs:300-300` — 处理入站 UDP 报文，支持多播/广播 fanout 和 UDP 分段重组。
  - B: `src/net/socket/udp.rs:20-20` — UDP socket 封装，通过 UDP_PORT_MAP 全局表实现端口复用，含 is_reuse_other 标志。

### 3.10 general：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**general 层是内核的平台无关抽象层，核心抽象是固件电源控制描述符与执行器（PowerControlInfo/PowerControlMethod/PowerRegister）以及 DTB/ACPI 固件表解析器。

### 3.11 hal：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**HAL 层是内核中隔离架构差异的薄抽象层，通过 kernel_symbols 宏导出稳定符号，将 loongarch64/riscv64 两套 arch 实现统一暴露给上层。核心抽象包括：中断保存/恢复（interrupt.rs）、时间源（time.rs）、平台元信息（platform.rs）、用户态 trap-frame 封装（user_context.r。

### 3.12 libs：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**libs 目录是内核的独立库集合，核心抽象包括：native-abi（MyGO Native ABI 的机器身份注册表与固定线格式）、elm（可加载模块的 EBI/ELM 协议与运行时模型）、soyo（直接可执行格式的 wire 定义与解析）、kcsan（采样式数据竞争检测运行时）。

### 3.13 src / kernel：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**src_kernel 是内核核心模块的聚合根，通过 constants.rs 集中定义 Linux ABI 常量（errno、ioctl、信号、statx、mmap 标志等），供 syscall、vfs、task 等子系统共享。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T202610112999587-1186` | B：人工指定历史候选 `T202510247995532-2817` |
| --- | --- | --- |
| 技术家族 | rcore-rust | rcore-rust |
| 系统调用数量 | 352 | 458 |
| 描述模块 | 12 | 10 |
