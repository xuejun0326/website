# T202610486999578-173 与人工指定历史候选 T202510003995291-2331 对比分析报告

- A（目标作品）：`T202610486999578-173`
- B（人工指定历史候选）：`T202510003995291-2331`
- 候选身份：年份=unknown、学校=unknown、队伍=unknown
- 候选身份来源：`metadata_unavailable`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **38.9%**，对应“低度相似”。

模块级对照覆盖 **11** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | 信号机制 | 85% | 做法接近 |
| 2 | 驱动框架 | 80% | 做法接近 |
| 3 | 内存管理 | 78% | 做法接近 |
| 4 | 文件系统 | 68% | 目标相同、实现路径不同 |
| 5 | 系统调用层 | 65% | 目标相同、实现路径不同 |
| 6 | 进程与任务调度 | 65% | 目标相同、实现路径不同 |
| 7 | 进程间通信 | 60% | 目标相同、实现路径不同 |
| 8 | 启动流程 | 55% | 目标相同、实现路径不同 |
| 9 | 网络 | 12% | 独立设计 |
| 10 | modules / lwext4 / rust | 0% | 仅历史候选实现 |
| 11 | patches | 0% | 仅目标作品实现 |

## 三、逐模块源码证据

### 3.1 信号机制：85%

**对照判断：**做法接近。 双层信号管理、固定跳板、统一 check_signals 高度一致；差异在检查时机与 sigreturn 防重入，整体同源。

#### 相似点

- **固定虚拟地址信号跳板**
  - A: `kernel/src/task/mod.rs:341-341` — 进程级信号管理器持有 SIGNAL_TRAMPOLINE 跳板地址
  - B: `core/src/mm.rs:35-38` — map_trampoline 映射固定地址跳板，属性 R+X+U
- **统一 check_signals 分发入口**
  - A: `kernel/src/task/signal.rs:18-18` — check_signals 统一检查并分发待决信号
  - B: `api/src/imp/task/signal.rs:25-25` — check_signals 处理 Terminate/CoreDump/Stop/Handler 分支

#### 主要差异

- **信号检查时机与触发路径**
  - A: `kernel/src/task/user.rs:39-57` — syscall 返回后临时恢复 pre-syscall mask 并检查信号，另有多处轮询点
  - B: `api/src/imp/task/signal.rs:70-74` — 注册为 POST_TRAP 回调，每次 trap（陷入，即异常、中断或系统调用进入内核的统一路径） 返回前统一检查
- **rt_sigreturn 后防 handler 重入**
  - A: `kernel/src/syscall/signal.rs:207-211` — 先设置 block_next_signal 再 restore 上下文防止重入
  - B: `api/src/imp/task/signal.rs:294-297` — 直接从 SignalFrame 恢复 TrapFrame 和屏蔽字，无防重入标志

### 3.2 驱动框架：80%

**对照判断：**做法接近。 两仓均以 DeviceOps trait 将设备挂接到 /dev 伪文件系统，核心抽象和职责一致；差异主要在设备注册方式和块设备封装细节。

#### 实现概况

- **A：**以 ArceOS 生态 crate 为底层，在 kernel/src/pseudofs 下构建统一设备抽象层，DeviceOps trait 将字符/块设备暴露给 VFS（虚拟文件系统，即统一不同文件系统的接口层），并实现 /dev 下特殊设备。
- **B：**以 VFS 伪文件系统为核心，将设备建模为 /dev 字符设备节点，DeviceOps trait 提供 read_at/write_at/ioctl 等操作，并支持物理内存直映射。

### 3.3 内存管理：78%

**对照判断：**做法接近。 共享 AddrSpace/缺页/mmap（内存映射，即把文件或匿名内存映射进进程地址空间） 基础，设备映射路径与 COW（写时复制，即共享内存页只在写入时复制） 扩展不同，整体相似度较高。

#### 相似点

- **核心地址空间抽象一致**
  - A: `kernel/src/mm/aspace/mod.rs:21-25` — AddrSpace 聚合 va_range、MemorySet、PageTable
  - B: `arceos/modules/axmm/src/aspace.rs:19-19` — AddrSpace 包含相同三要素，核心结构一致
- **缺页处理结合 per-CPU 标记**
  - A: `kernel/src/mm/access.rs:258-275` — PAGE_FAULT 检查 accessing_user_memory 标志后处理
  - B: `src/mm.rs:14-27` — 按 PHYS_VIRT_OFFSET + per-CPU 标记区分访问来源
- **mmap 按 flags 选择策略**
  - A: `kernel/src/syscall/mm/mmap.rs:197-260` — 按 MAP_SHARED/PRIVATE 选择后端
  - B: `api/src/imp/mm/mmap.rs:130-145` — 处理 MAP_FIXED/NOREPLACE 等标志

#### 主要差异

- **设备内存映射路径不同**
  - A: `kernel/src/syscall/mm/mmap.rs:213-244` — 经 DeviceMmap 枚举分派到不同后端
  - B: `api/src/core/fs/imp/dev/framebuffer.rs:126-132` — virt_to_phys 后 map_linear 直映射

### 3.4 文件系统：68%

**对照判断：**目标相同、实现路径不同。 共享 FileLike/fd 表/slab inode/伪文件系统等核心设计，但 fd 表作用域、挂载机制和目录遍历差异明显。

#### 相似点

- **fd 表均用 FlattenObjects 稀疏表与读写锁**
  - A: `kernel/src/file/mod.rs:217-220` — FD_TABLE 用 scope_local 声明为 per-task，RwLock 保护 FlattenObjects。
  - B: `api/src/core/fs/fd.rs:48` — FdTable 用 spin::RwLock 保护 FlattenObjects 稀疏表。
- **tmpfs 均用 slab 管理 inode**
  - A: `kernel/src/pseudofs/fs.rs:88-119` — SimpleFsNode 使用 slab 分配 inode，Drop 时自动释放。
  - B: `api/src/core/fs/imp/tmp.rs:1-449` — MemoryFs 用 slab 管理 inode，release_inode 依赖 n_link 和强计数。
- **伪文件系统用 DirMaker 延迟构造动态节点**
  - A: `kernel/src/pseudofs/mod.rs:30` — NodeOpsMux 的 Dir 分支持有 DirMaker，用于动态目录构造。
  - B: `api/src/core/fs/pseudo/dynamic.rs:1-350` — DynamicFs 用 DirMaker 闭包延迟构造节点，共享静态子节点表。

#### 主要差异

- **fd 表作用域：per-task 与全局**
  - A: `kernel/src/file/mod.rs:217-220` — FD_TABLE 是 per-task 独立 fd 表。
  - B: `api/src/core/fs/fd.rs:176` — fd_lookup 从全局 FD_TABLE 获取对象。
- **挂载机制：简单挂载 vs 挂载点栈**
  - A: `kernel/src/pseudofs/mod.rs:69-83` — mount_all 依次挂载 /dev、/dev/shm、/tmp、/proc、/sys。
  - B: `modules/vfs/src/mount.rs:1-287` — Mountpoint 形成挂载点栈，effective_mountpoint 处理覆盖挂载。
- **目录遍历：直接输出 vs sink 回调**
  - A: `kernel/src/pseudofs/dir.rs:142-160` — read_dir 先输出 . 和 ..，再通过 child_names 获取子项。
  - B: `modules/vfs/src/node/dir.rs:1-90` — 目录遍历采用 sink 回调模式，accept 返回 false 即停止。

### 3.5 系统调用层：65%

**对照判断：**目标相同、实现路径不同。 共同采用handle_syscall+Sysno match分发和寄存器参数接口，但入口上下文、stub策略和trap注册方式明显不同，故相似度中等。

#### 相似点

- **系统调用分发入口与匹配机制**
  - A: `kernel/src/syscall/mod.rs:31-40` — handle_syscall通过Sysno枚举match分发
  - B: `src/syscall.rs:28-30` — 同样用Sysno枚举match分发
- **参数提取接口**
  - A: `kernel/src/syscall/mod.rs:31` — UserContext提供arg0-arg5寄存器接口
  - B: `src/syscall.rs:29` — TrapFrame提供arg0-arg5方法
- **与信号处理的协同**
  - A: `kernel/src/task/mod.rs:87-97` — Thread保存last_syscall_no供信号处理
  - B: `api/src/imp/task/signal.rs:70-76` — trap返回前检查pending信号

#### 主要差异

- **入口上下文类型不同**
  - A: `kernel/src/syscall/mod.rs:31-40` — handle_syscall接收UserContext可变引用
  - B: `src/syscall.rs:28-30` — handle_syscall接收TrapFrame和syscall_num
- **未知调用处理策略不同**
  - A: `kernel/src/syscall/mod.rs:39-54` — Sysno::new失败回退到原始编号处理特例
  - B: `src/syscall.rs:522-525` — 未实现调用stub_bypass返回Ok(0)
- **入口注册方式不同**
  - A: `patches/axcpu/src/x86_64/init.rs:26-33` — 通过init_syscall配置syscall入口
  - B: `src/syscall.rs:28-30` — 属性宏注册到trap框架

### 3.6 进程与任务调度：65%

**对照判断：**目标相同、实现路径不同。 两者均以 TaskExt 桥接 axtask 并设共享状态容器，进程/线程语义一致；但表管理、类型层次等实现路径差异明显。

#### 相似点

- **基于 axtask TaskExt 桥接调度器**
  - A: `kernel/src/task/mod.rs:202-213` — on_enter/on_leave 切换 ActiveScope，连接调度器与用户任务
  - B: `core/src/task.rs:19-23` — def_task_ext! 宏把 TaskExt 挂到 axtask 任务上
- **进程级共享状态容器 ProcessData**
  - A: `kernel/src/task/mod.rs:235-266` — ProcessData 聚合地址空间、凭据、信号、futex（快速用户态互斥量，即用户态原子操作配合内核等待唤醒的同步机制） 等共享状态
  - B: `core/src/process.rs:19-30` — ProcessData 持有地址空间、信号管理器、futex 表等线程组共享状态
- **进程组/会话映射跟踪**
  - A: `kernel/src/task/ops.rs:28-31` — 四张 WeakMap 表维护 TID/PID（进程标识符，即系统用于区分进程的编号）/PGID/SID
  - B: `process/src/process_group.rs:6` — ProcessGroup 结构体承载会话与进程组映射

#### 主要差异

- **任务/进程表管理策略不同**
  - A: `kernel/src/task/ops.rs:247-266` — do_exit 每 64 次触发 WeakMap cleanup，清理过期条目
  - B: `process/src/process.rs:234-236` — PID 单调递增不回收，冲突直接 panic
- **POSIX（可移植操作系统接口，即 Unix 类系统通用接口规范） 类型层次 vs 表驱动映射**
  - A: `kernel/src/task/ops.rs:28-31` — 仅以表记录 TID/PID/PGID/SID 对应关系
  - B: `process/src/process_group.rs:6` — 显式 ProcessGroup 类型及 Session 层次结构

### 3.7 进程间通信：60%

**对照判断：**目标相同、实现路径不同。 共享内存与 futex 核心机制共同，但 A 多消息队列且段表结构不同，路径明显分化，相似度中等。

#### 相似点

- **共享内存全局管理器与映射表**
  - A: `kernel/src/syscall/ipc/shm.rs:287-300` — ShmManager 用三张 BTreeMap 映射表管理段
  - B: `core/src/shared_memory.rs:27-27` — SharedMemoryManager 持 Mutex<BTreeMap> 段表
- **sys_shmat 共享内存映射**
  - A: `kernel/src/syscall/ipc/shm.rs:480-480` — 映射段到进程地址空间，用 Backend::new_shared
  - B: `api/src/interface/mm/shm.rs:69-69` — 将段映射到当前进程地址空间
- **sys_futex 核心操作**
  - A: `kernel/src/syscall/sync/futex.rs:74-74` — 分发 WAIT/WAKE/REQUEUE 等命令
  - B: `api/src/imp/task/futex.rs:18-18` — 分发 WAIT/WAKE/REQUEUE 等命令

#### 主要差异

- **消息队列实现差异**
  - A: `kernel/src/syscall/ipc/msg.rs:339-352` — sys_msgget 创建/获取消息队列
  - B: `src/syscall.rs:459-469` — msgget 等映射到 stub_bypass 桩
- **共享内存段表结构差异**
  - A: `kernel/src/syscall/ipc/shm.rs:287-300` — 三张映射表：key↔shmid、shmid→inner、pid↔vaddr
  - B: `core/src/shared_memory.rs:27-27` — 单张 BTreeMap 段表，key 到 SharedMemory

### 3.8 启动流程：55%

**对照判断：**目标相同、实现路径不同。 两仓共享 main 入口、文件系统挂载与用户程序启动框架，但平台启动、ELF 加载与参数构造差异明显，综合相似度 55。

#### 相似点

- **均以 main 为内核入口并挂载文件系统后启动用户程序**
  - A: `src/main.rs:37-48` — 构造 sh -c 参数，调用 entry::init
  - B: `src/main.rs:27` — 初始化根文件系统与挂载，调用 run_user_app
- **均创建用户进程/任务并加载可执行文件**
  - A: `kernel/src/entry.rs:20-38` — 解析 init 可执行文件，创建首个用户进程并 spawn
  - B: `core/src/entry.rs:12` — 创建地址空间、加载 ELF、建立任务

#### 主要差异

- **A 含底层汇编启动，B 从 main 开始**
  - A: `patches/axplat-loongarch64-qemu-virt/src/boot.rs:71-88` — _start 汇编入口设置 DMW、MMU（内存管理单元，即完成虚拟地址到物理地址转换的硬件） 后进入 Rust
  - B: `src/main.rs:27` — B 的启动流程以 main() 为入口，不包含平台汇编
- **用户程序加载策略不同**
  - A: `kernel/src/entry.rs:20-38` — 解析 init 可执行文件，含 busybox 回退链
  - B: `core/src/mm.rs:108-160` — 脚本和 PT_INTERP 递归调用 load_user_app 加载
- **参数构造方式不同**
  - A: `src/main.rs:37-48` — 构造含 LTP 白/黑名单的 sh -c 参数
  - B: `core/src/entry.rs:33` — UspaceContext 硬编码初始参数 2333

### 3.9 网络：12%

**对照判断：**独立设计。 双方模块职责存在对应关系，但描述中的接口和实现组织重合较少。

#### 相似点

- **实现位置与职责对照**
  - A: `patches/axnet-ng/src/tcp.rs:300-300` — 从 ListenTable 取已连接 socket handle，构造新 TcpSocket 返回。
  - B: `api/src/imp/net/socket.rs:29-32` — Socket 枚举用 Mutex 包裹 axnet 的 UdpSocket/TcpSocket，作为统一套接字抽象。
- **实现位置与职责对照**
  - A: `patches/axnet-ng/src/tcp.rs:30-30` — POSIX 风格 TCP 套接字，封装 smoltcp（面向嵌入式环境的轻量网络协议栈） tcp::Socket，管理状态机和监听/连接。
  - B: `api/src/imp/net/socket.rs:278-278` — 创建 TCP/UDP 套接字并注册到 fd 表，返回 fd。
- **实现位置与职责对照**
  - A: `patches/axnet-ng/src/udp.rs:380-380` — 延迟绑定 UDP 套接字，在 connect/send 时分配临时端口并绑定 smoltcp socket。
  - B: `api/src/imp/net/socket.rs:98-106` — UDP sendto 前强制绑定回环地址临时端口，硬编码 127.0.0.1 限制发送源地址。

### 3.10 modules / lwext4 / rust：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**该模块将 C 语言实现的 lwext4 (ext2/3/4 文件系统库) 通过 FFI 封装为 Rust 接口, 供 ArceOS 内核使用。核心抽象是 Ext4BlockDevice (块设备适配层, 实现 ext4_blockdev_iface 回调) 与 Ext4Filesystem (文件系统主入口, 封装 ext4_fs 结构)。

### 3.11 patches：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**patches 目录是 Starry 对 ArceOS 的定制补丁集合，核心抽象是 axio 的 I/O trait 体系（Read/Write/BufRead/IoBuf/IoBufMut）与 axcpu 的架构抽象层，以及新增的 loongarch64 平台支持。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T202610486999578-173` | B：人工指定历史候选 `T202510003995291-2331` |
| --- | --- | --- |
| 技术家族 | arceos-starry | arceos-starry |
| 系统调用数量 | 257 | 212 |
| 描述模块 | 10 | 10 |
