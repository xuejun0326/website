# T2026104869910625-2955 与人工指定历史候选 T202410487992456-422 对比分析报告

- A（目标作品）：`T2026104869910625-2955`
- B（人工指定历史候选）：`T202410487992456-422`
- 候选身份：年份=unknown、学校=unknown、队伍=unknown
- 候选身份来源：`metadata_unavailable`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **27.8%**，对应“低度相似”。

模块级对照覆盖 **15** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | 内存管理 | 80% | 做法接近 |
| 2 | 启动流程 | 75% | 目标相同、实现路径不同 |
| 3 | 信号机制 | 65% | 目标相同、实现路径不同 |
| 4 | 进程与任务调度 | 65% | 目标相同、实现路径不同 |
| 5 | 系统调用层 | 62% | 目标相同、实现路径不同 |
| 6 | 驱动框架 | 58% | 目标相同、实现路径不同 |
| 7 | 进程间通信 | 50% | 目标相同、实现路径不同 |
| 8 | 文件系统 | 45% | 目标相同、实现路径不同 |
| 9 | include | 42% | 目标相同、实现路径不同 |
| 10 | 网络 | 16% | 独立设计 |
| 11 | hal | 0% | 仅目标作品实现 |
| 12 | hsai | 0% | 仅目标作品实现 |
| 13 | kernel | 0% | 仅目标作品实现 |
| 14 | kernel / platform | 0% | 仅历史候选实现 |
| 15 | kernel / src | 0% | 仅历史候选实现 |

## 三、逐模块源码证据

### 3.1 内存管理：80%

**对照判断：**做法接近。 双方均采用 VMA（虚拟内存区域，即一段具有相同属性的进程地址区间） 抽象和按需缺页处理，文件映射机制对应良好，仅在 VMA 存储方式等细节存在差异。

#### 相似点

- **VMA 抽象管理虚拟地址空间**
  - A: `kernel/vma.c:28-36` — VMA 记录地址、权限、类型，循环链表按地址排序
  - B: `include/mm/vma.h:8` — VMA 含地址、权限、类型，挂接在 mm_struct
- **缺页处理按需分配物理页**
  - A: `hsai/hsai_trap.c:83-117` — 检查地址映射后分配物理页并建立映射
  - B: `kernel/src/mm/pagefault.c:27-56` — 按需分配物理页，VMA_FILE 类型从文件读取
- **支持文件映射**
  - A: `kernel/vma.c:349-385` — mmap（内存映射，即把文件或匿名内存映射进进程地址空间） 文件映射逐页从文件读取内容
  - B: `kernel/src/mm/pagefault.c:27-56` — page_fault 中 VMA_FILE 类型从文件读取填充

#### 主要差异

- **VMA 存储方式不同**
  - A: `kernel/vma.c:28` — VMA 结构体用双向循环链表组织
  - B: `kernel/src/mm/vma.c:15-32` — VMA 存于编译期定长数组 vmas[NVMA]

### 3.2 启动流程：75%

**对照判断：**目标相同、实现路径不同。 共享 xv6 式多核启动框架与初始化顺序，但 boot hart 选举、M 态降级和从核唤醒路径明显不同。

#### 相似点

- **汇编入口为每核分配独立启动栈**
  - A: `hal/riscv/entry.S:10-28` — 按 hartid+1 计算栈顶，每核独立栈
  - B: `kernel/platform/qemu/entry/entry.S:4-14` — 同样按 hartid+1 乘 4KB 得到栈偏移
- **主从 hart 分叉，从 hart 等待标志**
  - A: `kernel/sos_start_kernel.c:64-87` — CAS 选举 boot hart，非 boot hart 自旋等待 riscv_kernel_ready
  - B: `kernel/platform/qemu/src/main.c:9-51` — first 标志区分主从，从 hart 自旋等待 started
- **内存屏障保证初始化可见后置位标志**
  - A: `kernel/sos_start_kernel.c:64-87` — boot hart 通过 __sync_synchronize() 后置位就绪标志
  - B: `kernel/platform/qemu/src/main.c:53-56` — 主 hart 内存屏障后置位 started

#### 主要差异

- **start() 职责：M 态降级 vs 仅传 hartid**
  - A: `hal/riscv/start.c:29-68` — 完成 M→S 降级、中断委托、PMP 配置后 mret
  - B: `kernel/platform/qemu/src/start.c:10-13` — 仅保存 hartid 到 tp 并调用 main，忽略 dtb
- **从 hart 唤醒：自旋等待 vs SBI 主动唤醒**
  - A: `kernel/sos_start_kernel.c:64-87` — 非 boot hart 自旋等待标志，wfi 空转
  - B: `include/main.h:32-44` — start_all_harts 通过 SBI 主动唤醒从 hart

### 3.3 信号机制：65%

**对照判断：**目标相同、实现路径不同。 共同采用位图信号集与返回路径惰性投递，但信号表粒度、pending 队列及默认行为差异明显。

#### 相似点

- **位图信号集**
  - A: `include/kernel/signal.h:50-66` — 用单个 unsigned long 表示 64 位信号集
  - B: `include/ipc/signal.h:90-95` — 同样用 64 位位图表示信号集
- **返回路径惰性投递**
  - A: `kernel/syscall.c:3325-3330` — 系统调用返回前检查 pending 位图并投递
  - B: `kernel/platform/qemu/src/trap.c:129` — trap（陷入，即异常、中断或系统调用进入内核的统一路径） 返回前调用 signal_handle 分发
- **线程级信号发送**
  - A: `kernel/process.c:1345-1362` — tgkill 设置线程 pending 位
  - B: `kernel/src/proc/tcb_life.c:219-224` — thread_send_signal 入队并唤醒线程

#### 主要差异

- **信号处理表粒度**
  - A: `kernel/thread.c:57-58` — 线程仅持屏蔽字与 pending，处理表在进程级
  - B: `kernel/src/proc/tcb_life.c:210-216` — 每线程独立分配带锁和引用计数的 sighand
- **pending 队列结构**
  - A: `kernel/process.c:1290-1315` — kill 仅置位 pending 位图，无排队信息
  - B: `include/ipc/signal.h:75-84` — 位图+链表双结构，保存 siginfo 支持排队
- **不可屏蔽信号集合**
  - A: `kernel/signal.c:5-19` — 强清 SIGTERM/SIGKILL/SIGSTOP 屏蔽位
  - B: `kernel/src/ipc/signal.c:36-39` — 仅强清 SIGKILL/SIGSTOP 屏蔽位

### 3.4 进程与任务调度：65%

**对照判断：**目标相同、实现路径不同。 双方均基于xv6扩展多线程，共享协作式调度、clone和独立栈，但调度器结构差异大（链表轮询vs状态队列），核心路径不同。

#### 相似点

- **线程独立内核栈与swtch切换**
  - A: `kernel/process.c:713-760` — clone_thread为子线程分配独立内核栈和trapframe，sched切换线程上下文
  - B: `kernel/src/proc/tcb_life.c:69-78` — 新线程上下文指向fn，内核栈顶为kstack+PGSIZE，thread_sched使用swtch切换
- **协作式yield主动让出CPU**
  - A: `kernel/process.c:640-650` — yield将进程和主线程置为RUNNABLE后切换，用于时钟中断轮转
  - B: `kernel/src/proc/sched.c:60` — thread_yield将状态改为RUNNABLE并调用thread_sched切换
- **clone系统调用创建线程**
  - A: `kernel/process.c:713-760` — 新增clone_thread系统调用，支持CLONE_SETTLS等标志
  - B: `kernel/src/proc/pcb_life.c:322-359` — do_clone支持CLONE_THREAD创建线程或CLONE_VM共享地址空间

#### 主要差异

- **调度器结构：链表轮询vs状态队列**
  - A: `kernel/process.c:457-520` — 轮询进程池，在进程的线程队列中寻找可运行线程
  - B: `kernel/src/proc/sched.c:127-143` — 直接从runnable_t_q取线程，O(1)获取，状态队列映射
- **线程管理结构：队列链表vs线程组**
  - A: `kernel/process.c:192-200` — allocproc创建主线程挂入thread_queue链表，进程内维护线程队列
  - B: `kernel/src/proc/tcb_life.c:202` — 引入thread_group结构，包含组锁、组领导和线程链表管理多个线程

### 3.5 系统调用层：62%

**对照判断：**目标相同、实现路径不同。 共同采用 Linux 编号和 trapframe 分发，但 switch-case 与函数指针表、参数范围和 trapframe 设计差异明显。

#### 相似点

- **Linux 系统调用号**
  - A: `include/kernel/syscall_ids.h:4-13` — 采用 Linux 标准编号，busybox/glibc 可直接运行
  - B: `kernel/src/syscall/syscall_table.c:2-4` — 以 Linux 系统调用号为数组下标，指定初始化器映射
- **trapframe 读取 a7 分发**
  - A: `kernel/syscall.c:3689-3699` — hsai_get_arg 提取 a0-a7 后按 a[7] 分发
  - B: `kernel/src/syscall/syscall.c:132-143` — 从 trapframe 的 a7 读取系统调用号并调用处理函数
- **陷入入口区分系统调用**
  - A: `hsai/hsai_trap.c:583-593` — usertrap 根据 scause 判断系统调用后调用 syscall
  - B: `kernel/platform/qemu/src/trap.c:58-89` — thread_usertrap 区分 scause==8 系统调用、中断和缺页

#### 主要差异

- **分发机制 switch vs 表**
  - A: `kernel/syscall.c:3689-3699` — 巨型 switch-case 直接分发，无函数指针表
  - B: `kernel/src/syscall/syscall_table.c:2-4` — syscalls[284] 函数指针表驱动分发
- **参数寄存器支持数量**
  - A: `hsai/hsai_trap.c:301-318` — hsai_get_arg 支持 0-7 号参数寄存器
  - B: `kernel/src/syscall/syscall.c:40-53` — arg_raw 仅读取 a0-a5，超 6 个参数 panic
- **trapframe 设计差异**
  - A: `include/hsai/trap.h:33-38` — RISC-V 前 5 字段为内核上下文，LoongArch 布局不同
  - B: `include/kernel/trap.h:26-48` — trapframe 额外保存 f0-f31 和 fcsr 浮点上下文

### 3.6 驱动框架：58%

**对照判断：**目标相同、实现路径不同。 共享 VirtIO（虚拟输入输出接口，即虚拟机中使用的标准设备协议） 描述符环、struct disk 与初始化流程, 但同步机制(忙等vs信号量)、抽象层与驱动范围差异显著。

#### 相似点

- **VirtIO 描述符环与 struct disk**
  - A: `kernel/driver/loongarch/virtio_disk.c:60-60` — struct disk 页对齐存放 desc/avail/used 与 free/info/ops
  - B: `kernel/platform/qemu/src/virtio_disk.c:17-28` — 相同结构集中描述符环与在途请求跟踪, 页对齐
- **VirtIO 设备初始化流程**
  - A: `kernel/driver/loongarch/virtio_disk.c:300-305` — la_virtio_disk_init 做 PCI（外设组件互连，即枚举和访问设备的总线标准） 配置、capability 解析与队列配置
  - B: `kernel/platform/qemu/src/virtio_disk.c:76-79` — virtio_disk_init 校验 magic/version、协商 features、配置队列并初始化信号量

#### 主要差异

- **同步与完成通知机制**
  - A: `kernel/driver/loongarch/virtio_disk.c:590-594` — 忙等状态字节判断完成, 无超时
  - B: `kernel/platform/qemu/src/virtio_disk.c:217-235` — 提交后释放锁, 阻塞在 per-buffer 信号量等待中断唤醒
- **平台无关抽象层**
  - A: `kernel/driver/loongarch/virtio_disk.c:501-504` — 直接暴露架构相关的 la_virtio_disk_rw 接口
  - B: `kernel/platform/qemu/src/disk.c:6-13` — 通过平台无关 disk_ 接口屏蔽 virtio/ramdisk 差异
- **补充存储驱动不同**
  - A: `kernel/driver/loongarch/sata.c:241-249` — SATA(AHCI) 驱动作为补充存储路径, 轮询完成
  - B: `kernel/src/driver/ramdisk.c:22-30` — ramdisk 驱动作为默认存储路径, 含冗余 memmove

### 3.7 进程间通信：50%

**对照判断：**目标相同、实现路径不同。 双方均实现管道与futex（快速用户态互斥量，即用户态原子操作配合内核等待唤醒的同步机制）基础语义，但管道阻塞与futex队列结构差异明显，B另有共享内存支持。

#### 相似点

- **共同实现futex等待/唤醒机制**
  - A: `kernel/futex.c:16-37` — 定长数组等待队列，支持WAIT/WAKE/REQUEUE
  - B: `kernel/src/atomic/futex.c:204-229` — futex_wait检查用户值后挂起到等待队列
- **均提供管道环形缓冲区读写**
  - A: `kernel/fs/pipe.c:26-49` — pipealloc分配环形缓冲区与两个文件对象
  - B: `include/ipc/pipe.h:1` — struct pipe含环形缓冲区与读写指针

#### 主要差异

- **管道阻塞同步机制不同**
  - A: `kernel/fs/pipe.c:112-180` — 写满时sleep_on_chan原子释放锁睡眠
  - B: `kernel/src/ipc/pipe.c:63-91` — 写满时释放锁并阻塞在write_sem信号量
- **futex等待队列数据结构不同**
  - A: `kernel/futex.c:7` — futex_queue_t全局定长数组项
  - B: `include/atomic/futex.h:1` — struct futex含自旋锁与等待队列

### 3.8 文件系统：45%

**对照判断：**目标相同、实现路径不同。 共享 lwext4 间接块限制与 HTree 校验和缺陷，但页缓存、多文件系统与数据结构差异显著，相似度中等。

#### 相似点

- **lwext4 间接块限制计算**
  - A: `kernel/fs/ext4_fs.c:91-97` — 计算间接块上限，存在 64KB 块下溢出风险
  - B: `kernel/src/fs/ext4/lwext4/ext4_fs.c:61-87` — 读取超级块并计算 4 级间接块上限
- ****
  - A: `kernel/fs/ext4_dir_idx.c:233-242` — 明确跳过校验和验证，仅警告
  - B: `kernel/src/fs/ext4/lwext4/ext4_dir_idx.c:233-257` — 注释未实现校验，失败仅警告

#### 主要差异

- **文件系统支持范围不同**
  - A: `kernel/fs/vfs_ext4.c:210-210` — 定义文件系统操作表并注册为 EXT4_FS_OP
  - B: `kernel/src/fs/fat32/fat32_inode.c:30-45` — 根 inode 注册 FAT32 多态操作表，支持多 FS
- **页缓存与预读机制差异**
  - A: `kernel/fs/ext4.c:1568-1581` — ext4_fread 直接读取，无页缓存
  - B: `kernel/src/fs/vfs/filemap.c:42-66` — do_generic_read 页缓存预读批量读入
- **核心数据结构差异**
  - A: `kernel/fs/ext4.c:1545-1545` — ext4_file 句柄封装 inode/fpos/fsize
  - B: `kernel/src/fs/fat32/fat32_inode.c:28-28` — 通用 inode 结构，含 i_op 与页缓存映射

### 3.9 include：42%

**对照判断：**目标相同、实现路径不同。 核心锁与定时器接口同源于 xv6，但头文件组织、架构支持与容器实现差异显著，相似度中等。

#### 相似点

- **自旋锁接口与调试字段**
  - A: `include/kernel/spinlock.h:8-22` — spinlock_t 含 locked/name/cpu 字段，声明 push_off/pop_off
  - B: `include/atomic/spinlock.h:6-15` — struct spinlock 含 locked/cpu/name，acquire/release 宏注入调用位置
- **定时器抽象接口**
  - A: `include/kernel/timer.h:12-18` — 定义 100MHz/100Hz 时钟与 TIME2NS 转换宏
  - B: `include/lib/timer.h:23-40` — 定义 timer_list 软定时器与 add_timer_atomic 接口

#### 主要差异

- **架构支持范围差异**
  - A: `include/kernel/context.h:6-42` — 条件编译区分 RISC-V s0-s11 与 LoongArch s0-s8+fp
  - B: `include/kernel/trap.h:26-80` — trapframe 仅针对 RISC-V 布局，与 trampoline.S 对应
- **头文件组织方式**
  - A: `include/kernel/defs.h:1-16` — 集中声明各 .c 函数，是 xv6 经典方式
  - B: `include/common.h:1-20` — 以 ifndef USER 条件编译汇聚基础类型与宏，Linux 风格分散
- **容器宏实现来源**
  - A: `include/kernel/queue.h:125-133` — FreeBSD 风格 SLIST/STAILQ/LIST/TAILQ 宏
  - B: `include/lib/list.h:8-22` — Linux 风格 list_head 双向循环链表与 container_of

### 3.10 网络：16%

**对照判断：**独立设计。 双方模块职责存在对应关系，但描述中的接口和实现组织重合较少。

#### 相似点

- **实现位置与职责对照**
  - A: `include/kernel/socket.h:55-55` — 模拟网络数据包，记录目标端口、数据指针和有效标志，用于进程间数据传递。
  - B: `kernel/src/syscall/syscall.c:132-132` — 系统调用分发入口，从 trapframe->a7 取编号，查表调用；未注册的 socket 调用返回 -1。
- **实现位置与职责对照**
  - A: `include/kernel/socket.h:30-30` — socket 核心结构，包含协议族/类型/协议、状态机、本地/远程地址、超时与 pending 连接标志。
  - B: `kernel/src/syscall/syscall.c:12-17` — 参数解析通过 copy_in 从用户态地址空间安全拷贝，做了边界保护；未来实现 socket 系统调用可复用此框架。
- **实现位置与职责对照**
  - A: `kernel/syscall_net.c:330-330` — 接受连接，检查 pending_conn 标志并调用 alloc_connected_socket_fd 创建新 socket fd。
  - B: `kernel/src/syscall/syscall_table.c:2-2` — 系统调用函数指针数组，大小硬编码 284；socket 相关编号槽位为 NULL，未注册处理函数。

### 3.11 hal：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**hal 模块是双架构 (RISC-V 与 LoongArch) 的底层汇编与硬件抽象层，核心抽象是 trampoline 跳板页、kernelvec 内核陷入入口、swtch 上下文切换、TLB 重填例程以及 SBI 调用封装。它解决用户态与内核态切换时页表切换、寄存器保存/恢复、时钟中断注入等硬件相关机制。

### 3.12 hsai：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**hsai 模块是内核的打印/异常诊断与陷入处理子系统，核心抽象包括：基于自旋锁保护的 printf 格式化输出、panic/assert 宏体系、LoongArch 异常码表（exception_table）驱动的异常描述查询。

### 3.13 kernel：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**本仓库内核核心模块以 vmem.c 的页表管理为骨架，提供多级页表 walk/mappages/vmunmap、用户地址空间 copyin/copyout 以及 uvmalloc/uvmcopy 等内存原语；console.c 实现基于环形缓冲与睡眠锁的字符设备。

### 3.14 kernel / platform：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**该模块是 CabbageOS 的平台启动与硬件抽象层，包含 qemu 与 visionfive 两个平台变体。核心抽象是 main() 启动序列、entry.S 多核引导、linker.ld 内存布局、trap.c 中断/异常分发、bio.c 块缓存与 disk.c 磁盘抽象。

### 3.15 kernel / src：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**本模块涵盖 xv6 血缘内核中未被九大固定模块认领的基础设施：格式化输出（printf/vsprintf/vsnprintf）、快速排序（qsort）、字符分类（ctype）、通用队列（queue）、原子操作（atomic）、CPU 管理（cpu.c）以及汇编层陷入/上下文切换（trampoline.S/kernelvec.S/swtch.S）和磁盘镜像嵌入。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T2026104869910625-2955` | B：人工指定历史候选 `T202410487992456-422` |
| --- | --- | --- |
| 技术家族 | unix-c | xv6-c |
| 系统调用数量 | 121 | 288 |
| 描述模块 | 13 | 12 |
