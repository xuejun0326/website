# T202610486999845-3024 与人工指定历史候选 T202510213995926-2475 对比分析报告

- A（目标作品）：`T202610486999845-3024`
- B（人工指定历史候选）：`T202510213995926-2475`
- 候选身份：年份=unknown、学校=unknown、队伍=unknown
- 候选身份来源：`metadata_unavailable`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **35.4%**，对应“低度相似”。

模块级对照覆盖 **10** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | 信号机制 | 78% | 做法接近 |
| 2 | 系统调用层 | 65% | 目标相同、实现路径不同 |
| 3 | 内存管理 | 60% | 目标相同、实现路径不同 |
| 4 | 启动流程 | 55% | 目标相同、实现路径不同 |
| 5 | 进程与任务调度 | 45% | 目标相同、实现路径不同 |
| 6 | 进程间通信 | 40% | 目标相同、实现路径不同 |
| 7 | 网络 | 35% | 目标相同、实现路径不同 |
| 8 | 驱动框架 | 35% | 目标相同、实现路径不同 |
| 9 | 文件系统 | 20% | 目标相同、实现路径不同 |
| 10 | os-la | 0% | 仅目标作品实现 |

## 三、逐模块源码证据

### 3.1 信号机制：78%

**对照判断：**做法接近。 共同基于 trap（陷入，即异常、中断或系统调用进入内核的统一路径） 返回延迟投递、位图与动作表管理，系统调用接口高度重合；差异主要在实时信号队列支持。

#### 相似点

- **trap 返回路径延迟投递**
  - A: `os/src/syscall/mod.rs:1224-1226` — maybe_deliver_signal 在 trap 返回路径检查并投递 pending 信号
  - B: `os/src/signal/mod.rs:52-67` — handle_signal 在返回用户态前取信号并修改 trap 上下文跳转 handler
- **信号编号范围 1-64**
  - A: `os-la/src/syscall/process.rs:2503-2510` — 投递前检查信号编号在 1..=64 内
  - B: `os/src/signal/sig_struct.rs:9-15` — SigPending 位图支持 64 个信号，含 32 个实时信号
- **标准信号系统调用接口**
  - A: `os/src/syscall/mod.rs:174-184` — 映射 kill/tgkill/sigaction/sigprocmask/sigreturn 等系统调用
  - B: `os/src/syscall/signal.rs:280` — sys_rt_sigaction 安装/查询信号动作并校验信号合法性

#### 主要差异

- **实时信号队列支持差异**
  - A: `os-la/src/syscall/process.rs:2503-2510` — 仅检查 1..=64 号信号，未实现实时信号排队语义
  - B: `os/src/signal/sig_struct.rs:9-15` — SigPending 用 BTreeMap 记录 SigInfo，支持 32 个实时信号

### 3.2 系统调用层：65%

**对照判断：**目标相同、实现路径不同。 共同点：统一分发入口、由 trap 层调用；差异：返回类型、参数个数、调度集成方式。

#### 相似点

- **统一 syscall 分发入口**
  - A: `os/src/syscall/mod.rs:612` — syscall() 接收 trap 传入参数并分发
  - B: `os/src/syscall/mod.rs:353` — syscall() 为唯一入口，match 分发到各子模块
- **由 trap 层进入内核后调用**
  - A: `os/src/syscall/mod.rs:612` — 接收 trap 层传入编号与参数
  - B: `os/src/syscall/mod.rs:1-7` — 文档说明由 trap_handler 中的 ecall 调用

#### 主要差异

- **返回类型设计不同**
  - A: `os/src/syscall/mod.rs:426-431` — SyscallResult 枚举，含 Value/Switch 两种返回
  - B: `os/src/syscall/errno.rs:1` — SyscallRet 即 Result<usize, Errno>
- **参数传递个数不同**
  - A: `os/src/syscall/mod.rs:612` — 签名 args: [usize; 6]，6 个参数
  - B: `os/src/syscall/mod.rs:352-362` — 接收 8 个寄存器值，_a6 显式忽略
- **调度协同策略不同**
  - A: `os/src/syscall/mod.rs:622-628` — clone 通过 Switch 返回子进程 TrapContext，syscall 层完成切换
  - B: `os/src/syscall/net.rs:346-358` — syscall_connect 调用 yield_current_task 主动让出 CPU

### 3.3 内存管理：60%

**对照判断：**目标相同、实现路径不同。 仅有 SV39 页表与懒分配等基础机制相同，地址空间组织与帧管理实现路径完全不同。

#### 相似点

- **均基于 SV39 三级页表管理用户地址空间**
  - A: `os/src/task/user.rs:184-194` — 页表和物理帧均为编译期静态数组，通过 align(4096) 保证页对齐
  - B: `os/src/mm/memory_set.rs:88-95` — MemorySet 持有 PageTable 并用 BTreeMap 管理虚拟区域
- **预留地址区懒分配，缺页时再建映射**
  - A: `os/src/task/user.rs:1925-1928` — 缺页时分配物理帧并写 PTE，是懒分配核心入口
  - B: `os/src/mm/area.rs:200-205` — FilebeRO/Stack 类型 map 直接 panic，完全依赖缺页懒分配

#### 主要差异

- **地址空间管理采用静态账本而非区域对象**
  - A: `os/src/task/user.rs:13-24` — 以裸常量直接操作 PTE 位，用户 VPN→帧映射存于全局静态数组
  - B: `os/src/mm/memory_set.rs:88-95` — 以 BTreeMap<VirtPageNum, MapArea> 组织地址空间，brk（堆边界调整，即通过移动边界扩展或收缩进程堆）/heap/mmap（内存映射，即把文件或匿名内存映射进进程地址空间） 分别跟踪
- **物理帧分配：静态帧池计数 vs 栈式分配器**
  - A: `os/src/task/user.rs:258` — 帧引用计数用 u16 存储，saturating_add 防溢出但饱和后会泄漏
  - B: `os/src/mm/frame_allocator.rs:79-88` — StackFrameAllocator 优先从 recycled 栈取页，否则从 current 指针递增
- **页表示：裸 PTE 常量 vs Page/PageKind 抽象层**
  - A: `os/src/task/user.rs:13-24` — 直接定义 SV39 页表项标志位常量，无封装类型
  - B: `os/src/mm/page.rs:24-28` — PageKind 统一匿名页、文件页缓存和 inline 数据三种页类型

### 3.4 启动流程：55%

**对照判断：**目标相同、实现路径不同。 共同拥有汇编入口和BSS清零流程，但多核启动机制（SBI HSM vs 原子标志）与架构支持差异显著，相似度中等。

#### 相似点

- **汇编入口跳转至Rust主函数**
  - A: `os/src/entry.asm:1-17` — _start加载栈顶后call rust_main
  - B: `os/src/main.rs:71-84` — fake_main内联汇编修正sp后跳转rust_main
- **rust_main统一进行BSS清零**
  - A: `os/src/main.rs:49-55` — rust_main中调用clear_bss清零低端BSS
  - B: `os/src/main.rs:89-104` — 引导核执行clear_bss等初始化

#### 主要差异

- **多核启动机制不同**
  - A: `os/src/main.rs:57-87` — 通过SBI hart_start显式拉起worker hart并忙等ready
  - B: `os/src/main.rs:89-104` — 通过IS_BOOT compare_exchange竞争决定引导核

### 3.5 进程与任务调度：45%

**对照判断：**目标相同、实现路径不同。 两模块均有 per-hart 调度与汇编切换，但存储模型与调度策略差异显著：A 静态数组固定容量，B 动态 Task+CFS/FIFO。

#### 相似点

- **per-hart 调度状态隔离**
  - A: `os/src/task/user.rs:568-583` — 每个 hart 独立保存调度状态与迁移标记
  - B: `os/src/task/scheduler.rs:56-60` — 调度器按 hart 索引存储，无锁访问依赖单核独占
- **汇编级上下文切换**
  - A: `os/src/task/user.rs:129-140` — 进入用户态前将内核 callee-saved 寄存器存全局
  - B: `os/src/task/scheduler.rs:183-201` — schedule 切换 Processor.current 后调用 __switch
- **阻塞/挂起任务状态保存**
  - A: `os/src/task/user.rs:445-457` — 挂起进程 kernel stack 可复用，须在栈外保存 trap frame
  - B: `os/src/task/manager.rs:230-241` — wait 将当前任务标记为可中断并加入等待队列

#### 主要差异

- **静态数组 vs 动态 Task**
  - A: `os/src/task/user.rs:109-111` — 编译期固定容量静态数组，超限即失败
  - B: `os/src/task/task.rs:89-95` — Task 结构体 repr(C)，tp 直接指向其地址
- **多核任务分配策略**
  - A: `os/src/task/user.rs:668-683` — hart1 主循环协作取线程，无线程自旋等待
  - B: `os/src/task/scheduler.rs:164-167` — select_cpu 用 Relaxed 原子轮转分配 CPU，无负载均衡

### 3.6 进程间通信：40%

**对照判断：**目标相同、实现路径不同。 两仓均实现shm/futex（快速用户态互斥量，即用户态原子操作配合内核等待唤醒的同步机制）/pipe，职责相同；但A仓用全局数组+credit记账，B仓用哈希队列+真实等待，机制差异显著，故分数中等。

#### 相似点

- **共享内存系统调用支持**
  - A: `os/src/syscall/process.rs:4102` — sys_shmget直接操作全局SHM_SEGMENTS数组
  - B: `os/src/mm/shm.rs:187-190` — ShmManager用RwLock<BTreeMap>管理共享内存段
- **futex系统调用实现**
  - A: `os/src/syscall/process.rs:6832-6849` — sys_futex处理WAIT分支，比较用户态值
  - B: `os/src/futex/futex.rs:261-283` — futex_wake持桶锁唤醒任务并移除队列
- **管道实现**
  - A: `os/src/syscall/fs.rs:1644-1647` — pipe状态用static mut全局数组管理
  - B: `os/src/fs/pipe.rs:511-521` — PipeRingBuffer用环形缓冲区维护head/tail

#### 主要差异

- **IPC（进程间通信，即进程之间交换数据和同步状态的机制）对象管理方式**
  - A: `os/src/syscall/process.rs:1509` — IPC全局状态在程序加载时整体清零
  - B: `os/src/mm/shm.rs:187-190` — RwLock<BTreeMap>动态管理，支持延迟删除
- **futex等待机制**
  - A: `os/src/syscall/process.rs:709` — 用固定credit数组记账，无真实等待队列
  - B: `os/src/futex/queue.rs:13-22` — 256个哈希桶，每个桶独立Mutex，存储FutexQ
- **共享内存权限与生命周期**
  - A: `os/src/syscall/process.rs:4102` — sys_shmget无权限检查，最小实现
  - B: `os/src/mm/shm.rs:27-32` — ShmSegment包含IpcPerm权限，marked_for_deletion延迟删除

### 3.7 网络：35%

**对照判断：**目标相同、实现路径不同。 A 无协议栈纯模拟，B 基于 smoltcp（面向嵌入式环境的轻量网络协议栈） 真实协议栈，仅 accept 队列等表面机制相似。

#### 相似点

- **待接受队列机制**
  - A: `os/src/syscall/fs.rs:9019-9027` — 环形队列存储 pending peer 连接
  - B: `os/src/net/mod.rs:635-642` — 驱动层解析 SYN+ACK 注入 ListenTable
- **地址绑定/配置**
  - A: `os/src/syscall/fs.rs:7841-7878` — sys_bind 校验非回环/特权端口/自动分配
  - B: `os/src/net/mod.rs:79-85` — IP/网关/DNS 编译期硬编码

#### 主要差异

- **协议栈实现方式**
  - A: `os/src/syscall/fs.rs:6068-6110` — 明确不接入 virtio（虚拟输入输出接口，即虚拟机中使用的标准设备协议）-net，fd 层模拟
  - B: `os/src/net/mod.rs:411-411` — SocketSetWrapper 封装 smoltcp SocketSet
- **数据面模型**
  - A: `os/src/syscall/fs.rs:1407-1436` — Socket 内固定 4096 字节单包缓冲
  - B: `os/src/net/mod.rs:520-520` — NetDeviceWrapper 实现 Device trait 直接收发以太网帧

### 3.8 驱动框架：35%

**对照判断：**目标相同、实现路径不同。 两边均实现块设备驱动，但接口抽象、缓存机制、设备发现差异显著，仅共用读写接口与 VirtIO 支持等少量相似点。

#### 相似点

- **块设备读写接口**
  - A: `os/src/drivers/virtio.rs:210` — read_sectors 提交 virtqueue 请求读取连续扇区
  - B: `os/src/drivers/block/block_dev.rs:3` — trait 定义 read_blocks/write_blocks 块读写接口
- **支持 VirtIO 块设备**
  - A: `os/src/drivers/virtio.rs:145` — VirtioBlock 封装 virtio-mmio 设备寄存器与 virtqueue
  - B: `os/src/drivers/block/mod.rs:1-17` — VirtIO 块设备作为 feature 后端之一，BlockDeviceImpl 指向 VirtIOBlock
- **对上层隐藏硬件细节**
  - A: `os/src/drivers/mod.rs:1-8` — 窄接口导出 VirtioBlock 和 SECTOR_SIZE，ext4 只依赖这两个符号
  - B: `os/src/drivers/block/block_dev.rs:3` — trait 抽象，上层文件系统只需调用 read_blocks/write_blocks

#### 主要差异

- **接口抽象粒度不同**
  - A: `os/src/drivers/virtio.rs:145` — 具体结构体 VirtioBlock 暴露 new/read_sector/read_sectors 窄接口
  - B: `os/src/drivers/block/block_dev.rs:3` — trait BlockDevice 定义 read_blocks/write_blocks，支持多后端动态替换
- **是否带缓存层**
  - A: `os/src/drivers/virtio.rs:275-279` — 轮询等待设备完成，无缓存直接读写
  - B: `os/src/drivers/block/block_cache.rs:114-135` — BlockCacheManager 用 LRU 和 Arc 引用计数淘汰缓存
- **设备发现方式不同**
  - A: `os/src/drivers/virtio.rs:294-301` — 通过 read_satp 判断分页状态选择 MMIO 基地址
  - B: `os/src/drivers/mod.rs:33-63` — 解析设备树 reg 属性获取 MMIO 基地址并映射到内核空间

### 3.9 文件系统：20%

**对照判断：**目标相同、实现路径不同。 共同点是只读 EXT4 解析与 syscall 读写路径；但 A 为直接耦合的最小实现，B 为类 Linux VFS（虚拟文件系统，即统一不同文件系统的接口层） 分层，差异主导。

#### 相似点

- **根文件系统挂载初始化**
  - A: `os/src/fs/ext4.rs:139-139` — Ext4 结构体直接持有 VirtioBlock 并挂载只读镜像
  - B: `os/src/fs/mount.rs:181-181` — do_ext4_mount 初始化根文件系统并挂载 devfs/procfs/tmpfs
- **文件描述符读写路径**
  - A: `os/src/syscall/fs.rs:259-281` — 句柄类型区分 EXT4 文件与内存虚拟文件，构成 fd 抽象
  - B: `os/src/fs/file.rs:22-31` — File 封装 offset/path/inode/flags，实现 FileOp trait 提供读写

#### 主要差异

- **VFS 分层 vs 直接耦合**
  - A: `os/src/fs/ext4.rs:1-8` — 模块只实现只读 EXT4 子集，无统一文件系统 trait
  - B: `os/src/fs/mod.rs:11-27` — 以 mod.rs 为根暴露 dentry/file/inode/mount 等子模块，类 Linux VFS 分层
- **路径缓存与权限检查**
  - A: `os/src/fs/ext4.rs:29-35` — 块缓存 static mut 直接映射，无路径缓存、无并发保护
  - B: `os/src/fs/dentry.rs:305-318` — Dentry 路径缓存 children 用 Weak 防循环，支持负目录项

### 3.10 os-la：0%

**对照判断：**仅目标作品实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **B：**未实现该模块。
- **A：**os-la 是面向 LoongArch64 QEMU 评测环境的独立内核实现，核心抽象是 UPSafeCell 单核内部可变性封装、SBI/MMIO 直写控制台、以及基于 EXT4 镜像的 ELF 加载器与评测 harness。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T202610486999845-3024` | B：人工指定历史候选 `T202510213995926-2475` |
| --- | --- | --- |
| 技术家族 | rcore-rust | rcore-rust |
| 系统调用数量 | 285 | 262 |
| 描述模块 | 10 | 9 |
