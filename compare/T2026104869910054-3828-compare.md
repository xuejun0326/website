# T2026104869910054-3828 与人工指定历史候选 T202510247995532-2817 对比分析报告

- A（目标作品）：`T2026104869910054-3828`
- B（人工指定历史候选）：`T202510247995532-2817`
- 候选身份：年份=unknown、学校=unknown、队伍=unknown
- 候选身份来源：`metadata_unavailable`

## 一、对比结论

按本次可用维度计算，目标作品与人工指定历史候选的整体相似度为 **25.2%**，对应“低度相似”。

模块级对照覆盖 **10** 个标准模块。下一节按模块相似度从高到低排列。

## 二、模块相似度

模块分数由双方已有模块描述、接口、数据结构和源码位置综合形成。

| 排名 | 模块 | 相似度 | 对照结论 |
| ---: | --- | ---: | --- |
| 1 | 文件系统 | 65% | 目标相同、实现路径不同 |
| 2 | 网络 | 65% | 目标相同、实现路径不同 |
| 3 | 内存管理 | 60% | 目标相同、实现路径不同 |
| 4 | 启动流程 | 50% | 目标相同、实现路径不同 |
| 5 | 驱动框架 | 50% | 目标相同、实现路径不同 |
| 6 | 信号机制 | 45% | 目标相同、实现路径不同 |
| 7 | 系统调用层 | 45% | 目标相同、实现路径不同 |
| 8 | 进程间通信 | 40% | 目标相同、实现路径不同 |
| 9 | 进程与任务调度 | 20% | 独立设计 |
| 10 | src / kernel | 0% | 仅历史候选实现 |

## 三、逐模块源码证据

### 3.1 文件系统：65%

**对照判断：**目标相同、实现路径不同。 共同：VFS（虚拟文件系统，即统一不同文件系统的接口层） 抽象、ext4 适配、统一查找；差异：挂载表 vs dcache、同步策略、文件系统集合，故 65 分。

#### 相似点

- **统一 VFS 抽象层**
  - A: `crates/fs/src/vfs/mod.rs:1-87` — 定义 FsKind/FsError 等统一接口
  - B: `src/fs/mod.rs:1-5` — 声明五个子模块构成 VFS 插件集合
- **均实现 ext4 并接入**
  - A: `crates/fs/src/init.rs:16-22` — 启动阶段挂载 ext4 等文件系统
  - B: `src/fs/ext4.rs:46-50` — 适配 BlockDevice 到 another_ext4 trait
- **统一文件查找入口**
  - A: `crates/fs/src/init.rs:242-267` — lookup_file 按优先级链查找文件
  - B: `src/kernel/vfs/dentry.rs:119-142` — 路径解析先查 dcache 再触发 lookup

#### 主要差异

- **挂载表静态数组 vs RCU dentry 缓存**
  - A: `crates/fs/src/vfs/mount.rs:10-14` — MountTable 用 static mut 固定数组存 8 挂载
  - B: `src/kernel/vfs/dentry.rs:41-51` — Dentry 用 RCU 指针保护实现无锁读
- **写回策略：后台延迟 vs 立即同步**
  - A: `crates/fs/src/init.rs:351-355` — background_writeback_step 周期刷脏页
  - B: `src/fs/ext4.rs:304-313` — write 后立即 sync，另带 10 秒惰性同步
- **文件系统集合不同**
  - A: `crates/fs/src/init.rs:16-22` — 挂载 ext4/procfs/ramfs/devfs/socketfs
  - B: `src/fs/mod.rs:1-5` — 声明 ext4/fat32/tmpfs/procfs/shm

### 3.2 网络：65%

**对照判断：**目标相同、实现路径不同。 共享 smoltcp（面向嵌入式环境的轻量网络协议栈） 栈与轮询机制，但 socket 建模、TCP 监听与 UDP 处理差异显著，故分中等。

#### 相似点

- **共享 smoltcp 协议栈**
  - A: `crates/net/src/iface.rs:30-60` — NetStack 持有 smoltcp Interface 和 SocketSet
  - B: `src/net/iface.rs:23-33` — Iface 封装 smoltcp Interface 与 SocketSet
- **全局网络栈实例管理**
  - A: `crates/net/src/iface.rs:180-190` — spin::Mutex 全局单例保护
  - B: `src/net/iface.rs:23-33` — IFACES 全局表按名管理多网卡
- **独立轮询任务驱动协议栈**
  - A: `crates/net/src/iface.rs:100-140` — poll worker 线程动态调整轮询间隔
  - B: `src/net.rs:71-77` — ifaces_poll 异步任务循环 poll

#### 主要差异

- **socket 对象模型不同**
  - A: `crates/net/src/sockets.rs:40-60` — NetSocketEntry 映射 (pid（进程标识符，即系统用于区分进程的编号）,fd) 到 SocketHandle
  - B: `src/net/socket/tcp.rs:20-20` — TcpSocket 异步封装，含 RwLock 字段
- **TCP 监听实现不同**
  - A: `crates/net/src/sockets.rs:330-380` — TcpListenPool 多 socket 监听同一端口实现 backlog
  - B: `src/net/socket/tcp.rs:336-341` — listen 未 bind 时 unimplemented!() panic，功能不完整
- **UDP 处理策略不同**
  - A: `crates/net/src/sockets.rs:700-760` — UdpRxQueue 按 logical_id 去重分发
  - B: `src/net/socket/udp.rs:27-27` — UDP_PORT_MAP 全局表实现端口复用

### 3.3 内存管理：60%

**对照判断：**目标相同、实现路径不同。 COW（写时复制，即共享内存页只在写入时复制）、引用计数、slab 分流策略一致，但帧分配算法（位图 vs 伙伴系统）与 TLB/页回收等机制差异明显。

#### 相似点

- **COW 写时复制机制**
  - A: `crates/mm/src/page_table.rs:428-434` — mprotect 遇到 COW 标志页保持写保护，缺页时复制。
  - B: `src/kernel/mem/mm_area.rs:93-115` — handle_cow 检查独占页，非独占则分配新页复制。
- **物理页引用计数管理**
  - A: `crates/mm/src/frame.rs:1-6` — 帧分配器位图+引用计数，refcount 归零才回收。
  - B: `src/kernel/mem/page_alloc/raw_page.rs:65-74` — RawPage 含 refcount 字段，跟踪页引用。
- **小对象 slab、大对象直接页分配**
  - A: `crates/mm/src/heap.rs:64-76` — kvmalloc 策略：小对象 slab，大对象 vmalloc，中等回退。
  - B: `src/kernel/mem/allocator.rs:16-33` — ≤2048 字节走 slab，大块直接分配页。

#### 主要差异

- **物理页分配算法不同**
  - A: `crates/mm/src/frame.rs:1-6` — 位图+引用计数实现帧分配器，无伙伴系统。
  - B: `src/kernel/mem/page_alloc.rs:58-73` — PerCpuPageAlloc 批量从全局伙伴系统补充页。
- **slab 页回收机制有无**
  - A: `crates/mm/src/slab.rs:66-80` — slab 初始化注册页回收器 reclaim_free_pages。
  - B: `crates/slab_allocator/src/slab_cache.rs:161-162` — TODO 未实现页回收水位检查，空 slab 页不归还。

### 3.4 启动流程：50%

**对照判断：**目标相同、实现路径不同。 共同采用 SBI HSM 多核启动与汇编入口，但交接抽象不同且 LoongArch 多核支持差异明显，故居中。

#### 相似点

- **RISC-V 多核启动均通过 SBI HSM 协议唤醒从核**
  - A: `crates/kernel/src/entry.rs:44-59` — 遍历非 boot hart，为每个从核计算独立栈顶并调用 SBI HSM 启动
  - B: `crates/eonix_hal/src/arch/riscv64/bootstrap.rs:241-281` — BSP 通过原子变量向 AP 传递分配器和栈地址，启动 AP
- **从核启动前均需等待同步信号**
  - A: `crates/kernel/src/main.rs:86-94` — LoongArch 从核自旋等待 LOONGARCH_BOOT_READY 置位
  - B: `crates/eonix_hal/src/arch/riscv64/bootstrap.rs:321-340` — AP 通过 AP_SEM 信号量互斥获取 AP_STACK 栈地址
- **入口汇编跳转到 Rust 主启动函数**
  - A: `crates/kernel/src/main.rs:16-24` — RISC-V 入口保存 hart id 后跳转 rust_main
  - B: `crates/eonix_hal/src/arch/riscv64/bootstrap.rs:78-95` — 置 satp 开启 Sv48 后跳转 riscv64_start

#### 主要差异

- **启动交接抽象不同：trait vs 结构体**
  - A: `crates/hal/src/common/boot.rs:5-14` — ArchBoot trait 定义 arch_init/shutdown/hart_start 等接口
  - B: `crates/eonix_hal/src/bootstrap.rs:5-17` — BootStrapData 结构携带早期栈与页分配器交接给内核
- **LoongArch 多核启动支持不同**
  - A: `crates/kernel/src/main.rs:86-94` — LoongArch 从核通过邮箱 IPI 唤醒并等待就绪标志
  - B: `crates/eonix_hal/src/arch/loongarch64/bootstrap.rs:296-297` — bootstrap_smp 为空实现，LoongArch 多核未完成

### 3.5 驱动框架：50%

**对照判断：**目标相同、实现路径不同。 职责相同且共享注册与中断机制，但抽象模型（Device vs PCI（外设组件互连，即枚举和访问设备的总线标准））与设备发现（DTB+PCI vs 仅PCI）明显不同，综合给 50 分。

#### 相似点

- **驱动注册静态表机制**
  - A: `crates/driver/src/registry.rs:3-6` — 全局注册表集中注册驱动，未来迁 linkme
  - B: `src/kernel/pcie/driver.rs:14-34` — 按 vendor/device ID 静态表注册并回放设备
- **中断处理路径**
  - A: `crates/driver/src/manager.rs:141-148` — 框架级 handle_irq 遍历设备分发中断
  - B: `src/driver/ahci/mod.rs:82-100` — AHCI 中断上下文处理端口状态并分发

#### 主要差异

- **设备发现机制差异**
  - A: `crates/driver/src/manager.rs:24-30` — 先注册驱动，再 DTB 探测，LoongArch64 额外 PCI 扫描
  - B: `src/kernel/pcie/driver.rs:14-34` — 仅依赖 PCI 枚举，注册后回放已枚举设备

### 3.6 信号机制：45%

**对照判断：**目标相同、实现路径不同。 共享POSIX（可移植操作系统接口，即 Unix 类系统通用接口规范）信号语义和trap（陷入，即异常、中断或系统调用进入内核的统一路径）返回前异步投递框架，但状态存储、信号框架与功能范围差异显著，故分数居中。

#### 主要差异

- **信号恢复机制：内核态 sigreturn vs vdso 跳板**
  - A: `crates/syscall/src/imp/signal.rs:426-432` — 架构相关 signal_frame_sp 布局，rt_sigframe 由内核管理
  - B: `src/kernel/task/signal/signal_action.rs:21-32` — vdso_sigreturn naked function，放 .vdso.sigreturn 段，用户态恢复

### 3.7 系统调用层：45%

**对照判断：**目标相同、实现路径不同。 同实现syscall分发，但机制差异大：A编译期match同步，B链接节查表异步；LoongArch64寄存器约定不同。

#### 相似点

- **syscall号到处理函数的分发机制**
  - A: `crates/syscall/src/dispatch.rs:40-44` — handle_syscall 通过 match 分发到各 imp 子模块
  - B: `src/kernel/syscall.rs:15-37` — 查表分发，链接节收集，MAX_SYSCALL_NO=512
- **从trap上下文提取参数并处理syscall**
  - A: `crates/kernel/src/trap_handler.rs:473-505` — kernel_trap_handler 推进PC后调用 handle_syscall 并设返回值
  - B: `src/kernel/task/thread.rs:418-418` — trap 层 Syscall 事件在 thread.rs 分发到内核 syscall 处理

#### 主要差异

- **编译期match分发 vs 宏+链接节查表**
  - A: `crates/syscall/src/dispatch.rs:40-44` — match 分发约200个syscall，无间接跳转开销
  - B: `src/kernel/syscall.rs:15-37` — 宏注册到 .raw_syscalls 链接节，启动时收集为查表
- **同步handler vs 异步Future handler**
  - A: `crates/syscall/src/dispatch.rs:40-44` — handle_syscall 返回 isize，同步调用
  - B: `src/kernel/syscall.rs:15-37` — handler 返回 Pin<Box<dyn Future + Send>>，异步执行
- **LoongArch64 syscall号寄存器不一致**
  - A: `crates/hal/src/la64/trap.rs:81-88` — 使用 r11 作为 syscall 号寄存器
  - B: `crates/eonix_hal/src/arch/loongarch64/trap/trap_context.rs:102-115` — 同样从 a7 读 syscall 号（与 riscv64 一致）

### 3.8 进程间通信：40%

**对照判断：**目标相同、实现路径不同。 共同核心为futex（快速用户态互斥量，即用户态原子操作配合内核等待唤醒的同步机制）与shmat接口，但key模型、共享内存后端及requeue支持差异显著。

#### 相似点

- **均以futex实现等待与唤醒**
  - A: `crates/task/src/futex.rs:349` — futex_wake_bitset按bitset匹配唤醒等待者
  - B: `src/kernel/task/futex.rs:193` — futex_wake唤醒指定futex上最多max_wake_count个等待者
- **均提供共享内存shmat映射**
  - A: `crates/syscall/src/imp/shm.rs:133` — sys_shmat将共享内存段映射到用户地址空间并记录VMA（虚拟内存区域，即一段具有相同属性的进程地址区间）
  - B: `src/kernel/syscall/mm.rs:216` — shmat通过mmap_fixed/mmap_hint将共享内存文件映射到用户空间

#### 主要差异

- **共享内存后端实现不同**
  - A: `crates/syscall/src/imp/shm.rs:99-102` — sys_shmget直接分配物理页并记录在Segment.phys数组
  - B: `src/fs/shm.rs:37-66` — ShmManager以tmpfs FileInode为载体，通过全局锁串行化操作
- **requeue支持程度不同**
  - A: `crates/task/src/futex.rs:434-442` — requeue操作通过比较桶索引大小决定加锁顺序，避免死锁
  - B: `src/kernel/task/futex.rs:216-258` — double_lock_bucket按升序加锁后futex_requeue直接todo!()，未实现

### 3.9 进程与任务调度：20%

**对照判断：**独立设计。 职责相同但机制独立，仅有进程/线程抽象和进程表共点，调度模型、并发控制与上下文切换完全不同。

#### 相似点

- **进程/线程双层抽象**
  - A: `crates/task/src/process.rs:180` — Process持线程表、子进程集、VMA与fd表
  - B: `src/kernel/task/process.rs:44` — Process为资源容器，Thread为可调度实体
- **全局任务/进程表**
  - A: `crates/task/src/scheduler.rs:96` — TaskTable以递增entry id索引Vec<Option<Box<TaskEntry>>>
  - B: `src/kernel/task/process_list.rs:10` — ProcessList用BTreeMap管理进程/线程/会话/进程组

#### 主要差异

- **调度模型：抢占式多核 vs 协作式async**
  - A: `crates/task/src/scheduler.rs:1-5` — 注释为协作式轮转，实际通过时钟中断实现抢占
  - B: `src/kernel/task/thread.rs:357-374` — real_run协作式循环，定时器中断时主动yield_now
- **上下文切换机制**
  - A: `crates/task/src/scheduler.rs:76` — TaskEntry含KernelContext与satp，切换时保存/恢复
  - B: `src/kernel/task/thread.rs:434-460` — contexted每次poll设置CURRENT_THREAD并激活/停用页表
- **并发控制设计**
  - A: `crates/task/src/scheduler.rs:118-120` — on_cpu原子字段以release序发布，支持跨CPU同步
  - B: `src/kernel/task/process.rs:507-515` — Proof类型强制持锁，RCU管理父子指针无锁读

### 3.10 src / kernel：0%

**对照判断：**仅历史候选实现。 只有一边实现该模块，未形成共同实现。

#### 实现概况

- **A：**未实现该模块。
- **B：**src_kernel 是内核核心模块的聚合根，通过 constants.rs 集中定义 Linux ABI（应用二进制接口，即程序与内核在二进制层面的调用约定） 常量（errno、ioctl、信号、statx、mmap（内存映射，即把文件或匿名内存映射进进程地址空间） 标志等），供 syscall、vfs、task 等子系统共享。

## 四、对比范围与方法

候选由用户人工指定；本报告只计算目标作品与该候选之间的相似度。
候选身份只从随仓库提供的元数据读取；未提供的字段明确标记为 unknown。
系统调用是用户程序请求内核服务的受控入口；下表只保留理解相似度所需的基础信息。

| 项目 | A：目标作品 `T2026104869910054-3828` | B：人工指定历史候选 `T202510247995532-2817` |
| --- | --- | --- |
| 技术家族 | rcore-rust | rcore-rust |
| 系统调用数量 | 195 | 458 |
| 描述模块 | 9 | 10 |
